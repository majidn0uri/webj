# اجرا رویِ کامپیوترِ خودت (مک یا لینوکس) — سه فرمان

```bash
git clone https://github.com/majidn0uri/sell-web.git
cd sell-web
bash setup.sh
```

پس ازِ آن: فروشگاه روی http://localhost:3100 و پنلِ مدیریت روی http://localhost:3100/admin
ورودِ نخستین: `09120000000` / `SetShop@1404` — **بی‌درنگ از پنل عوضش کن** (رمزِ نمونه در مخزن عمومی است).

---

## پیش‌نیاز

تنها چیزی که اسکریپت نصب **نمی‌کند**، خودِ Node است (چون نصبِ Node یعنی تصمیمِ
مدیریتِ نسخه — nvm/asdf/Homebrew — و نباید به تو تحمیل شود):

| لازم | حداقل | اگر نداری |
|---|---|---|
| Node | ۲۰.۱۱ (پیشنهاد ۲۴ LTS) | `brew install node` یا `nvm install 24 && nvm use 24` |
| npm | با Node می‌آید | — |
| git | هر نسخه | روی مک با `xcode-select --install` می‌آید |
| curl | روی مک و لینوکس هست | — |
| PostgreSQL | ۱۴ تا ۱۸ | لازم نیست؛ اسکریپت با brew/apt/dnf/pacman/zypper نصبش می‌کند |

`bash setup.sh check` را اول بزن: فقط می‌گوید چه چیزی کم است و هیچ چیزی نصب یا تغییر نمی‌دهد.

## چه کار می‌کند (به همان ترتیب)

۱. **پیش‌نیاز** — اگر PostgreSQL نبود، نصب می‌کند و هر دو مسیرِ رایج را می‌شناسد:
   پوشهٔ keg-onlyِ Homebrew در مک، و `/usr/lib/postgresql/<نسخه>/bin` در لینوکس.
۲. **وابستگی‌ها** — `npm ci` (و اگر نشد `npm install`).
۳. **رازها** — `internal-api-token` و `master-key` در `~/.setshop` با مجوز ۶۰۰.
   هیچ رازی به مخزن نمی‌رود. بی‌توکنِ درونی، معافیتِ سهمیه‌یِ تماسِ وب→API از کار
   می‌افتد و فروشگاه در شلوغی خودش را قفل می‌کند؛ بی‌`master-key` گاوصندوقِ
   گواهیِ مؤدیان باز نمی‌شود.
۴. **پایگاه‌داده** — خوشهٔ خصوصی در `~/.setshop/pgdata` روی پورت **۵۴۳۳**.
   چرا بیرون ازِ مخزن؟ چون `/tmp` گاه پاک می‌شود و `/.data` با هر پاک‌سازیِ مخزن
   می‌رود — هر دو را تجربه کرده‌ایم. چرا ۵۴۳۳؟ تا پستگرسِ خودت (پورت ۵۴۳۲)
   دست‌نخورده بماند.
۵. **مهاجرت‌ها** — هر ۳۴ مهاجرت، و **نشاندنِ کاتالوگ فقط وقتی پایگاه خالی است**
   (`COUNT(products)=0`)؛ تا کالاهایی که بعداً در پنل می‌سازی بازنویسی نشوند.
۶. **سرویس‌ها** — API روی ۳۰۰۰ و وب روی ۳۱۰۰. بی‌`--prod` با `next dev`
   (فوری، بی‌ساخت)؛ با `--prod` یک‌بار `next build` و بعد `next start`.

## پنج فرمانِ روزمره

```bash
bash setup.sh status        # چه چیزی بالا است (سرویس‌ها، پورت‌ها، سلامت)
bash setup.sh logs web -f   # لاگِ وب — جایش api و db هم همین‌طور
bash setup.sh stop          # خاموش کردنِ همه‌چیز (پایگاه هم؛ با --keep-db روشن می‌ماند)
bash setup.sh seed          # سفارش‌هایِ نمونه تا صفحه‌هایِ گزارش عدد نشان دهند
bash setup.sh --prod        # اجرایِ بهینه‌سازی‌شده (بعد ازِ هر تغییرِ بزرگِ کد)
```

گزینه‌هایِ دیگر: `--open` (مرورگر را باز می‌کند) · `--api-port=` / `--web-port=` ·
`--db-url=postgres://…` (پستگرسِ خودت) · `--env=my.env` (متغیرهایِ اضافه) ·
`--reset-db` (خوشهٔ توسعه از نو) · `--with-sales` (نشاندن + فروشِ نمونه) · `-y`.

## قالبِ سایت: یک فایلِ HTML که خودکار تازه می‌ماند

`docs/qalib.html` مرجعِ بصریِ کاملِ سایت است: سربرگ، هیرو، نوارِ اعتماد، کارت‌های
کالا، **و** همهٔ توکن‌هایِ طراحی (رنگ، فاصله، شعاع، سایه، تایپ) به‌همراهِ کتابخانهٔ
اجزا — همه از روی **همان CSS که سایت با آن رندر می‌شود** ساخته شده است. فونت و
تصویرها داخلِ خودِ پرونده‌اند؛ را به هر جایی ببر (ایمیل، لپ‌تاپِ بی‌اینترنت،
کارفرما) درست دیده می‌شود.

```bash
npm run template        # بازتولیدِ قالب — بعد ازِ هر تغییری در طراحی (storefront.css / globals.css)
npm run template:check  # فقط بررسی: کهنه باشد، خطا بدهد (همین را CI هم می‌زند)
```

قاعدهٔ کار: وقتی **طراحی** را عوض می‌کنی، همان commit که CSS را عوض می‌کند،
`npm run template` را بزن و `docs/qalib.html` را همراهِ آن commت کن. اگر فراموش
کردی، نگران نباش — دروازهٔ «کیفیت» گیت‌هاب (CI) با `template:check` متوجه
می‌شود که قالب کهنه است و merge نمی‌دهد. خودِ پرونده قابلِ ویرایشِ دستی
**نیست**؛ اگر دستش بدهی، بارِ بعدیِ `npm run template` آن را بازمی‌نویسد.

## پیامکِ واقعی: چهار کار، بی‌کد

فروشگاه پیامک‌ها را در صفِ `sms_outbox` می‌نویسد و یک «کارگر» آن‌ها را به
سامانه می‌فرستد. برایِ روشن‌کردنِ مسیرِ واقعی، هیچ چیزی در کد عوض نمی‌شود:

1. **حسابِ پیامکی** — کاوه‌نگار، ملی‌پیامک یا فراز. کلید/کاربری‌تان را بردارید.
2. **پنل → تنظیمات ← پیامک**: «ارائه‌دهنده» را انتخاب کنید، «کلیدِ ارسال» را
   پر کنید و «ارسالِ پیامک» را روشن کنید. همان‌جا «خطِ پیامک»، «حداکثرِ تلاش»
   و «فاصلهٔ تلاشِ دوباره» هم هست.
3. **قالب یا خطِ پیامک — یکی از این دو**: کاوه‌نگار/ملی‌پیامک/فراز هر سه «متنِ
   آزاد» می‌فرستند، به شرطِ این‌که «خطِ پیامک» را در همان صفحه پر کرده باشی. اگر
   می‌خواهی از **قالبِ تأییدشده** در پنلِ سامانه استفاده کنی (که برایِ پیامک‌هایِ
   انبوه لازم است)، «شناسهٔ قالب» را بنویس. بی‌هر دو، پیامک در صندوق می‌ماند و
   علتش را با نامِ هر دو درمان می‌نویسد — این قاعده را خودِ کارگر هم در همان
   دورِ اول فریاد می‌زند (خطِ «پیامک: …» در خروجی‌اش).
4. **کارگر** — همان چیزی که صف را خالی می‌کند. در نصبِ سرور، تایمرِ
   `setshop-postsale.timer` هر چند دقیقه یک بار یک دور می‌زند؛ رویِ ماشینِ خودت
   دو فرمانِ استاندارد هست (نیازی به کپی‌کردنِ `npx tsx` نیست):

```bash
npm run worker:once    # یک دور: صورتحساب‌هایِ جامانده، پیامک‌ها، گارانتی، پاک‌سازی، فایل
npm run start:worker   # چرخه: هر ۶۰ ثانیه یک دور (POSTSALE_INTERVAL_SEC=15 برایِ فاصلهٔ دیگر)
```

پیش از هر کاری، دو خطِ اولِ خروجی می‌گوید **چرا چیزی ارسال می‌شود یا نمی‌شود**
(«❌ هیچ سامانه‌ای انتخاب نشده…» / «⚠️ «ارسالِ پیامک» خاموش است» / «⚠️ کلیدِ دسترسی خالی است» /
«✅ این دور می‌فرستد · تا ۵ تلاش با فاصلهٔ ۱۰ دقیقه» و خطِ «صف: ۳ در انتظار…»).
`Ctrl-C` که بزنی دورِ جاری تمام می‌شود و تمیز می‌ایستی.

خروجیِ آخر چهار رقم دارد: `صورتحساب … | گارانتی … | پیامک: ۳ فرستاده شد (۰ دوباره
در صف، ۰ ناامیدکننده) | پاک‌سازی: ۰ سطرِ کهنه`. اگر «پیامک: ۰» بود و در صف چیزی نشسته بود، طبیعی است؛
اگر نشسته بود و هیچ‌کدام نرفت، `deploy/env.example` دو کلیدِ آزمایشی دارد
(`SMS_DRY_RUN=1` یعنی عمداً نفرست — خاموشش کنید).

### بی‌خریدِ اعتبار: کلِ مسیرِ پیامک را با یک سامانهٔ ساختگی بسنج

`scripts/fake-sms-provider.mjs` پاسخِ هر سه سامانه را بازی می‌کند و هر درخواست را
در `var/fake-sms.log` می‌نویسد — تا ببینی **دقیقاً چه چیزی** بیرون می‌رود، بی‌این‌که
پولِ اعتبار خرج شود یا اینترنتِ بین‌الملل باز باشد:

```bash
node scripts/fake-sms-provider.mjs &                      # روی 127.0.0.1:8099
SMS_ALLOW_INSECURE=1 SMS_KAVENEGAR_URL=http://127.0.0.1:8099/v1 npm run worker:once
```

`sent` با `provider_ref = FAKE-0001` یعنی کارگر تا سامانه رفت و برگشت. برایِ
دیدنِ «تلاشِ دوباره با فاصله»، متنِ پیام را با `FAIL` شروع کن: سرور ۵۰۰
می‌دهد و پیامک با `last_error = کدِ HTTP 500` تا دورِ بعد در صف می‌ماند.
دو کارگرِ هم‌زمان (تایمر + دستی) چیزی را دوباره نمی‌فرستند — برداشتنِ صف با
`SKIP LOCKED` و مهلتِ `sending` است؛ سنجیده شده (۲۴ پیام، ۴ کارگر: ۲۴ ارسال،
۲۴ ارجاعِ یکتا، هیچ تکراری).

**بی‌این‌که اینترنتِ بین‌الملل باز باشد هم کار می‌کند**: هر درخواستِ خروجی از
پالایشِ «فقط سرویسِ ایرانی» می‌گذرد و نشانیِ غیرمجاز، پیامک را رد می‌کند با
پیامی که نامِ همان قاعده را می‌گوید. برایِ آزمایشِ محلی با یک فرستندهٔ ساختگی،
`SMS_KAVENEGAR_URL` را روی لوکال بگذارید و `SMS_ALLOW_INSECURE=1` را اضافه کنید
(فقط برایِ همین آزمایش).

دکمهٔ «همین حالا بفرست» در پنل (تنظیمات ← صندوقِ پیامک) لازم نیست صبرِ تایمر را
بپذیرد: یک دورِ ارسال می‌زند و همان لحظه می‌گوید چندتا رفت و چرا نرفت. همان
«چرا نرفت» حالا در خطِ اولِ کارگر هم هست — چون پیش‌تر «آزمونِ پنل» سبز بود و
«صف» قرمزِ خاموش، و این دقیقاً همان چیزی بود که «پیامک وصل نیست» به‌نظر می‌رسید.

**اگر پیامک نمی‌رسد، اول تبِ «آزمونِ ارسال» را ببین** — دو کارت دارد: یکی
چک‌لیستِ «چرا پیامکی نمی‌رود؟» (ارائه‌دهنده، کلید، روشن‌بودنِ ارسال، DRY_RUN،
قالب‌هایِ بی‌شناسه، سنِ صف، قفلِ «در حالِ ارسال»ِ کارگرِ مرده) و دیگری خودِ
آزمون. اگر خانه‌ای را پر کردی و پیام‌هایِ قدیمی همچنان ایستاده‌اند، دکمهٔ
«بازگرداندنِ N پیام به صف» همان‌ست که لازم داری — تا پیش ازِ این، تنها راهش
ویرایشِ دستیِ جدول بود. پیامک‌هایِ فرستاده‌شده هرگز دست نمی‌خورند و خطاهایِ
سامانه‌ای (مثل «شماره در سیاه‌فهرست») بی‌زدنِ «اجباری» برنمی‌گردند.

**تنظیمات کامل است و همچنان چیزی نمی‌رود؟ تبِ «صندوقِ پیامک» را ببین.** بالایِ
همان تب یک کارتِ «پایشِ صف» هست: نمودارِ ستونیِ ساعتی (ارتفاع = پیامک‌هایی که در
آن ساعت در صف گذاشته شده‌اند، رنگ‌ها = فرستاده‌شده/در نوبت/ناموفق/ناامیدکننده)،
«آخرینِ ارسال»، «آخرینِ تکانهٔ صف» و — اگر پیامکِ نوبت‌دار باشد و هیچ تکانی
ندیده باشیم — بنرِ قرمزِ «کارگرِ صف خوابیده به‌نظر می‌رسد». این همان حالتی است که
تا پیش از این هیچ نشانه‌ای نداشت: تایمرِ systemd بعد ازِ یک آپدیت غیرفعال
می‌شود و صف بی‌صدا پُر می‌ماند.

رویِ سرور، سلامتِ خودِ تایمر را این‌طور چک کن:

```bash
systemctl is-active setshop-postsale.timer
systemctl cat setshop-postsale.timer | grep -i OnUnitActiveSec
```

عددِ «مهلتِ سکوتِ کارگرِ صف» (پیش‌فرض ۷۰ دقیقه) در تنظیمات ← پیامک قابل تغییر
است؛ آن را کمی بزرگ‌تر از فاصلهٔ اجرایِ تایمر بگذار (تایمرِ ۲ دقیقه‌ای ← ۱۵). اگر
کوچک‌تر از فاصلهٔ تایمر باشد، هر بار که صفحه را باز می‌کنی هشدارِ بی‌مورد
می‌بینی. برایِ دیدنِ خودِ داده رویِ خط: `GET /admin/settings/sms/stats?hours=48`
(با توکنِ مدیر) یا `npx tsx scripts/pg-stats-drill.mts` برایِ سنجشِ کاملِ مسیر.

**آزمونِ ارسال را این‌طور بزن** (تنظیمات ← پیامک ← آزمونِ
ارسال): یک پیامک به شمارهٔ خودش، با متنِ همان قالب و با پاسخِ خامِ سامانه زیرش.
«اعتبار تمام شده»، «قالب تأیید نشده»، «کلید غلط» و «شماره مسدود» همین‌جا از هم
جدا می‌شوند — بی‌این که مجبور شوی لاگِ سرویس را بخوانی. پنج آزمون در هر ده
دقیقه مجاز است، چون اعتبارِ واقعی مصرف می‌شود.

## پاک‌سازیِ خودکارِ داده: سه عدد که «سرورِ سنگین» را تعیین می‌کنند

پنلِ فروشگاهی که خودش را پاک نمی‌کند، بعد ازِ شش‌ماه رویِ همان ماشینِ قبلی
«سنگین» به‌نظر می‌رسد. سه عدد در **تنظیمات ← پیامک/پیشرفته** هستند و هر بار که
کارگر اجرا می‌شود از نو خوانده می‌شوند (بی‌restart):

| کلید | پیش‌فرض | چه چیزی را می‌برد |
|---|---|---|
| `sms_outbox_retention_days` | ۶۰ | پیامک‌های **تمام‌شده** (`sent`/`dead`) قدیمی‌تر از N روز |
| `observability_retention_days` | ۷ | شمارنده/رویدادِ پالایشِ نرخ (مهارِ بار) |
| `audit_log_retention_days` | **۰** | ردِّ عملیاتِ مدیریتی — **پیش‌فرض خاموش** |

سه قاعده که باید بدانی:

- **`0` یعنی «هرگز پاک نشود»** — نه «پیش‌فرض». اگر یک روز خواستی ممیزی
  را سبک کنی، عددِ روز بگذار (مثلاً ۷۳۰)، نه صفر. ممیزیِ عملیاتِ مالی
  تنها سندِ فروشنده در اختلافِ پرداخت است، پس پاک‌سازی‌اش عمدتاً opt-in است.
- **صفِ زنده هرگز دست نمی‌خورد:** فقط `sent` و `dead` واجدِ شرط‌اند. یک باگِ
  یک‌خطی در همین مسیر (تفاوتِ شرطِ شمارش با شرطِ حذف) پیامک‌هایِ `pending` را
  می‌کشت — آزمونِ «pendingِ ۵۰۰روزه باید بماند» برایِ همین هست.
- **یک دور، سقف دارد:** هر اجرا حداکثر `POSTSALE_PURGE_LIMIT` (پیش‌فرض ۲۰۰۰)
  سطر از هر جدول می‌برد و در گزارش «چقدر دیگر مانده» را می‌گوید. اگر صفِ
  پیامکِ انباشته داری، یک دورِ دستی با سقفِ بالاتر بزن:

```bash
DB_URL="postgres://setshop@/setshop?host=/tmp/setshop-pgrun&port=5433" \
POSTSALE_PURGE_LIMIT=20000 npx tsx scripts/post-sale-worker.ts
```

اول ببین چه چیزی قرار است برود، بعد بریزش دور — پیش‌نمایش بی‌حذف‌کردن، همان‌جا:

```bash
# GET /admin/observability/cleanup  → «چند سطر؟ چرا؟ هر جدول چقدر جا گرفته؟»
curl -s -H "Authorization: Bearer $TOKEN" localhost:3000/admin/observability/cleanup | jq
# DELETE نبود؛ برایِ اجرایِ واقعی: POST همان مسیر با {"limit":5000}
```

`dryRun` یعنی «نبینم، فقط بشمار». اگر خواستی بی‌پنل و دستی نگاه کنی چه چیزی
قرار است برود (از ریشهٔ مخزن اجرا کن؛ `-e` در tsx داخلِ تابع باید باشد چون
top-level await در این حالت کار نمی‌کند — این شکل تست شده و کار می‌کند):

```bash
DB_URL="postgres://setshop@/setshop?host=/tmp/setshop-pgrun&port=5433" npx tsx -e '
async function main() {
  const { createDatabase } = await import("@set/db");
  const { purgeExpiredData } = await import("@set/commerce");
  const db = createDatabase(process.env.DB_URL);
  console.log(JSON.stringify(await purgeExpiredData(db, { dryRun: true }), null, 2));
  await db.close();
}
main();'
```

خروجی برایِ هر جدول `removed` (در dryRun صفر می‌ماند)، `remaining` (چقدر دیگر
واجدِ شرط است)، `days` و کلیدِ تنظیمش را نشان می‌دهد — همان چیزی که در
پیش‌نمایشِ پنل هم می‌بینی. سنجشِ کاملِ این مسیر رویِ PostgreSQL واقعی:
`npx tsx scripts/pg-retention-drill.mts` و رویِ HTTP: `npx tsx
scripts/http-retention-drill.mts` (API باید بالا باشد؛ هر دو در پایان خودشان
را پاک می‌کنند).

## فایل‌هایِ رسانه: چرا دیسک پر می‌شود و چطور بی‌خطر تمیزش کنی

تصویرها در `MEDIA_DIR` می‌نشینند (پیش‌فرض: `var/media` در ریشهٔ مخزن؛ در استقرارِ
سرور `MEDIA_DIR=/var/lib/setshop/media`) و نامشان **درنگِ محتوا** است، پس دو کالایِ
هم‌عکس یک فایل را سه‌کاره‌اند. این یعنی «ردیفِ تصویر» با «فایل» یکی نیست و
پاک‌سازی باید هر دو را ببیند.

سه خانه در **تنظیمات ← پیشرفته**:

| کلید | پیش‌فرض | چه می‌کند |
|---|---|---|
| `media_grace_days` | ۱۴ | فایلِ بی‌صاحب چند روز بماند تا قطعی شود بی‌صاحب است؟ **۰ = هرگز** (و پوشه پویش هم نمی‌شود) |
| `media_autopurge` | خاموش | کارگرِ پس از فروش خودش بردارد؟ خاموش = فقط گزارش + دکمه |
| `media_scan_minutes` | ۳۶۰ | هر چند دقیقه یک بار پوشه از نو پویش شود |

اول ببین، بعد پاک کن — پیش‌نمایش بی‌حذف‌کردن:

```bash
curl -s -H "Authorization: Bearer $TOKEN" localhost:3000/admin/media/cleanup?fresh=1 | jq '.orphans, .orphanBytes, .missingTotal, .notes'
```

و اگر خواستی بدانی «یک دورِ کارگر چه می‌کند»:

```bash
DB_URL="postgres://setshop@/setshop?host=/tmp/setshop-pgrun&port=5433" \
POSTSALE_MEDIA_LIMIT=500 npx tsx scripts/post-sale-worker.ts
# خطِ آخرِ خروجی: «فایل: ۳ بی‌صاحب (۴٫۲ مگابایت) — خودکار خاموش»
```

سه چیز که باید بدانی، چون پاک‌کردنِ فایل برگشت ندارد:

- **بنر، دسته و اسکِنِ چک هم فایل می‌گیرند.** پاک‌سازیِ پیش از این فقط
  `product_images` را می‌دید و می‌توانست تصویرِ بنرِ امروز را بَرد؛ حالا هر شش
  ستونِ ارجاع شمرده می‌شود. اگر ستونِ تازه‌ای اضافه کردی، آزمونِ «پوششِ مراجع»
  تا فهرستش نکنی می‌شکند — این عمدی است.
- **فایلِ ناشناس پاک نمی‌شود.** هرچه نامش شکلِ `«درنگ»-«اندازه».jpg` نباشد فقط
  شمرده و گزارش می‌شود؛ پس اگر چیزی دستی داخلِ `MEDIA_DIR` ریخته‌ای، خودش
  می‌ماند و خودت درباره‌اش تصمیم می‌گیری.
- **اگر «بیش از نیمی از دیسک بی‌صاحب است» یا «پایگاه هیچ ارجاعی ندارد» دیدی،**
  سامانه چیزی پاک نمی‌کند و همان را می‌گوید. این دو معمولاً یعنی یا پایگاهِ
  دیگری به این پوشه وصل است یا پشتیبانِ کهنه‌ای بازگردانده شده — دقیقاً همان
  لحظه‌ای که «پاک‌سازیِ خودکار» باید بایستد. `force` هم هست (از پنل: همان دکمه با
  کلیدِ اجباری) و در ردِّ عملیات ثبت می‌شود.

سنجشِ کاملِ این مسیر: `npx tsx scripts/pg-media-drill.mts` (پایگاهِ واقعی +
فایل‌هایِ واقعی) و `npx tsx scripts/http-media-drill.mts` (از راهِ HTTP، با
بارگذاریِ واقعیِ تصویر؛ API باید بالا باشد). هر دو در پایان خودشان را پاک می‌کنند.

## جستجو: کشِ پرسش‌ها، مترادف‌ها، و «رم پر می‌شود»

### سه فرمان، اگر می‌خواهی خودت ببینی

پیش‌نیاز: API بالا باشد (`SMS_FAKE=1 npm run start:api` → `:3000`) و `DB_URL`
همان چیزی که `setup.sh` نوشته باشد. عددِ کالاها هرچه باشد کار می‌کند، ولی فرقِ
کار رویِ کاتالوگِ بزرگ دیده می‌شود:

```bash
DB_URL="postgres://setshop@/setshop?host=$HOME/.setshop/pgrun&port=5433" npx tsx scripts/seed-scale.mts --products 20000   # ≈۱۲ ثانیه
INTERNAL_API_TOKEN=$(cat ~/.setshop/internal-api-token) node scripts/measure-search.mjs --requests 300 --mode hot            # و --mode unique
DB_URL="postgres://setshop@/setshop?host=$HOME/.setshop/pgrun&port=5433" npx tsx scripts/seed-scale.mts --clean              # برگرداندن
```

برایِ بارِ هم‌زمان: `node scripts/load-test.mjs --users 40 --duration 20 --mix api
--json var/load-search-after.json`. **۴۰، نه ۱۵۰** — رویِ جعبهٔ دو‌هسته‌ایِ
توسعه، بارِ بیشتر اول خودِ ماشینِ آزمون را قفل می‌کند (تجربهٔ واقعی، با ده دقیقه
هیچ‌چیز) و عددِ جا‌مانده هیچی را ثابت نمی‌کند. اگر سرویس در این میانه از پا افتاد،
اسکریپت خودش با «فیوزِ اشباع» می‌ایستد. برایِ سنجشِ «با کش» و «بی‌کش» رویِ همان
سرورِ در حالِ اجرا، مقدارِ `search_cache_seconds` را عوض کن و هشت ثانیه صبر کن
(میکرو-کشِ سیاست پنج ثانیه است)؛ یا از پنل، دکمهٔ «خالی‌کردنِ کشِ جستجو» را بزن
که همان لحظه اعمال می‌شود.

#### سناریویِ پول: پرداخت و موجودی زیرِ بار (`--mix purchase`)

«توانِ فروشگاه» با دیدنِ صفحه سنجیده نمی‌شود؛ با جایی سنجیده می‌شود که دو خریدار
رویِ یک ردیفِ موجودی مسابقه می‌دهند و درگاه جواب می‌دهد. این سناریو همان چرخهٔ
شش‌فراخوانی است (سبد ← افزودن ← تسویه ← شروعِ پرداخت ← تصمیمِ درگاهِ آزمایشی ←
تأیید) و **پس از آزمون، حسابِ انبار را هم می‌بندد**: کسری باید دقیقاً برابرِ
فروشِ تأییدشده باشد و هیچ‌چه منفی نشود. سه دستور:

```bash
# ۱) آزمون (رویِ همان APIِ در حالِ اجرا). `-limiter` = ظرفیتِ خام، بدونِ مهارکنندهٔ نرخ
node scripts/load-test.mjs --users 25 --duration 20 --mix purchase --relax-limiter --json var/load-purchase.json

# ۲) تأییدِ مستقل از پایگاه — باید «۰» بدهد و `paid`ها با عددِ ابزار بخوانند
DB_URL="postgres://setshop@/setshop?host=$HOME/.setshop/pgrun&port=5433" psql -c \
  "SELECT count(*) FILTER (WHERE on_hand < 0) neg_on_hand, count(*) FILTER (WHERE reserved < 0) neg_reserved FROM stock_items;"

# ۳) برگرداندنِ دادهٔ آزمون از پنلِ فروشنده (موجودی هم پس می‌گیرد)
DB_URL="postgres://setshop@/setshop?host=$HOME/.setshop/pgrun&port=5433" psql -f scripts/purge-load-test-orders.sql
```

پیش‌نیازها: سرویسِ API بالا باشد (پیش‌آزماییِ ابزار خودش `404` را از `409`
جدا می‌کند)، صلاحیتِ `inventory.read` برایِ خواندنِ موجودیِ پس از آزمون — که با
ورودِ مدیرِ نمونه (`09120000000` / `SetShop@1404`) به‌دست می‌آید — و **موجودیِ
پُر** رویِ همان تنوعی که سناریو کشف می‌کند. اگر قفسه خالی باشد، ابزار می‌گوید
«این آزمون فقط ردِّ درست را می‌سنجد» و ادامه می‌دهد؛ فروشِ تأییدشده صفر خواهد بود.

| نشانه | علت و درمان |
|---|---|
| «پیش‌آزمایی شکست خورد: … → 403» رویِ تصمیمِ درگاه | در حالتِ **تولید** (`NODE_ENV=production`) درگاهِ آزمایشی عمداً قفل است: `PAYMENT_ALLOW_SANDBOX=1` لازم است. در `development` همین‌طور که هست باز است (§۲ف۷ در `docs/pishraft.md`) |
| «پیش‌آزمایی شکست خورد: … → 409» | انبارِ آن تنوع خالی است — آزمونِ پیشین یا `seed-scale` آن را خورده. از پنل (`/admin/inventory`) پرش کن یا `bash setup.sh db -y` بزن؛ ابزار خودش هم همین را می‌گوید و بی‌فروش ادامه می‌دهد |
| «پیامدِ فروش: ۰ پرداخت‌شده … ۷۰۰ ردِّ «موجودی نیست»» و حکمِ سبز | درست است: موجودی تمام شده و فروشگاه بیشتر از انبار نفروخته. برایِ دیدنِ فروش، موجودی را زیاد کن (مثلاً `200`) یا بارِ کمتر بگذار |
| همه‌جا `429` و «خطایِ واقعی بیش از ۱٪» | مهارکنندهٔ نرخ به‌ازایِ هر نشانی سهمیه دارد و خریدارهایِ ساختگی همه از یک IP‌اند. `--relax-limiter` (کلیدِ اضطراریِ پنل را می‌زند و در پایان **حتماً** برمی‌گرداند) |
| کندترین‌ها «خانه/برگه‌یِ کالا» با ۱۵ تا ۳۰ ثانیه و `timeout×N` | وب با `next dev` بالا است و هر برگه یک‌بار ترجمه می‌شود؛ ربطی به توانِ سرور ندارد. سناریو را بی‌`--with-pages` بزن، یا برایِ سنجشِ برگه‌ها `npm run build -w @set/web` |
| «❌ کسریِ انبار کمتر/بیشتر از فروشِ تأییدشده است» | یا کسرِ دوباره (رزرو **و** کم‌کردنِ موجودی در یک مسیر) یا سفارشِ پرداخت‌شده‌ای که انبار را کم نکرده. `exit code = 2` است تا در `CI` هم قرمز شود؛ اول `payments` را با `orders` بسنج |

### آنچه در پنل است (بی‌کد)

| کجا | چه |
|---|---|
| تنظیمات ← پیشرفته ← «عمرِ کشِ پرسش‌هایِ جستجو (ثانیه)» | پیش‌فرض ۱۵ و همان ۱۵ ثانیهٔ رندرِ برگهٔ نتایج است، پس کهنگیِ اضافه‌ای ساخته نمی‌شود. `0` = کش خاموش **و** خالی‌شدنِ کشِ مانده |
| جستجو و مترادف‌ها (`/admin/search`) | افزودنِ واژه‌ای که مشتری می‌نویسد ← واژه‌ای که در کالای تو آمده؛ خاموش/روشن و حذفِ هر سطر؛ جدولِ «مردم این‌ها را جستجو کردند و چیزی پیدا نشد» با دکمهٔ «مترادف بساز»؛ دکمهٔ «خالی‌کردنِ کشِ جستجو» |
| دیدبانی (`/admin/observability`) | `searchCache`: `hits/misses/entries/evictions/skippedBig/cleared` — «hits صفر با ترافیکِ بالا» خودش جواب است: یا کش خاموش است یا پرسش‌ها تکراری نیستند |

**قاعدهٔ کهنگی، تا سؤال نماند:** انتشارِ کالا، ویرایشِ کالا، تغییرِ سازگاریِ
مدلِ گوشی و هر مترادفِ تازه کش را **همان لحظه** می‌اندازند. چیزی که از سرویس
نمی‌گذرد (وارداتِ SQLی، `psql` دستی، اسکریپتِ همکار) تا عمرِ کش کهنه می‌ماند —
درِ فرارش همان دکمهٔ پنل است. موجودی و قیمتِ تغییرکرده با سفارش عمداً تا همان
۱۵ ثانیه می‌مانند؛ رندرِ برگهٔ نتایج هم از اول همین‌قدر کش داشت.

### `DB_POOL_MAX` (تنظیمِ سرور، نه پنل) — و چرا پیش‌فرض دیگر ۲۰ نیست

هر اتصالِ PostgreSQL یک بک‌اند است و هر بک‌اند ~۱۰۰ مگابایت رم. رویِ جعبهٔ
۱٫۹ گیگابایتی، استخرِ بیست‌اتصالی یعنی «رم پر می‌شود، صف می‌بندد، API هنگ
می‌کند» — دقیقاً همان نشانه‌ای که در توسعهٔ محلی می‌ترکید. حالا پیش‌فرض **دو
برابرِ هسته‌ها، بینِ ۴ و ۲۰** است و فقط اگر لازم شد دستی‌اش کن:

```bash
DB_POOL_MAX=16 npm run start:api     # سرورِ ۸ هسته‌ای، یا دو فرآیندِ API که باید سهمشان را بدانند
```

عددِ بی‌معنی (`''`، `0`، `-5`) نادیده گرفته می‌شود و به پیش‌فرض برمی‌گردد؛
`max: 0` استخری است که هیچ‌وقت اتصالی نمی‌دهد و اولین درخواستِ واقعی تا ابد در
صف می‌خوابد.

| نشانه | علت و درمان |
|---|---|
| جستجو درست کار می‌کند ولی «قیمت/عنوانِ تازه» دیر دیده می‌شود | طبیعی تا عمرِ کش (۱۵ ثانیه). از همان کالایِ پنل صفر بگذار، یا «خالی‌کردنِ کشِ جستجو» را بزن |
| کش روشن است ولی `hits` صفر می‌ماند | یا پرسش‌ها تکراری نیستند (معمولی در کاتالوگِ کوچک)، یا هر درخواست با گزینه‌هایِ متفاوت می‌آید (کلید، فیلترها و صفحه را هم می‌شمارد — درستِ همان است) |
| مترادف را افزودم و نتیجه عوض نشد | اگر **چندواژه** است: قاعده‌اش جایگزینی است، پس باید همان عبارت در پرسشِ مشتری بیاید (نه بخشی از آن). اگر تک‌واژه است: واژه باید بعد از نرمال‌سازی یکی شود — در همان صفحه `termKey → canonicalKey` را می‌بینی |
| `رم پر شد` یا `API هنگ کرد` وسطِ آزمونِ بار | بارِ بیش از جعبه (۴۰ کاربر کافی است) + استخرِ بزرگ. `DB_POOL_MAX=4`، بارِ کمتر، و `--abort-after` را دستکاری نکن |
| `npm run migrate` بی‌هیچ خروجی‌ای تمام شد | `DB_URL` نداشت؛ با `DB_URL=…` اجرا کن (پایگاهِ توسعه محلی: `postgres://setshop@/setshop?host=$HOME/.setshop/pgrun&port=5433`) |

## اگر گیر کرد

| نشانه | علت و درمان |
|---|---|
| `Node قدیمی است` | Node را تازه کن (جدولِ بالا) و دوباره اجرا کن. |
| `initdb پیدا نشد` بعد از brew install | `PGBIN=/opt/homebrew/opt/postgresql@17/bin bash setup.sh` |
| `پورت 3100 اشغال است` | `bash setup.sh --web-port=3300` (پورتِ خودِ پیش‌نمایش، تنظیمِ کد نیست) |
| پایگاه «نیمه‌بند»/خطای `pg_filenode.map` | `bash setup.sh --reset-db` — دادهٔ توسعه نشاندنی است، چیزی از دست نمی‌رود |
| صفحه‌ها باز می‌شوند اما کند | بی‌`--prod` هر صفحه یک‌بار در حالت dev ساخته می‌شود؛ برایِ سنجشِ سرعت `bash setup.sh --prod` |
| `sudo: a password is required` (لینوکس) | بسته‌بند را دستی اجرا کن: `sudo apt-get install -y postgresql` و بعد `bash setup.sh` |
| چند خریدارِ آخر با `429` رد می‌شوند | درست کار می‌کند: سقفِ «۱۰ سفارش در ساعت» برایِ هر نشانی. رویِ لپ‌تاپ همه از `127.0.0.1` می‌آیند، پس یکی می‌شود. برایِ سنجشِ دستی موقت خاموشش کن: `update rate_limit_rules set is_enabled=false where name='order.create'` ؛ یا از پنل: `/admin/observability` → همان‌جا «خاموش/روشن» هر سقف و «آزادکردنِ» یک سطل |
| منو/فیلترها بعد از **وارداتِ دسته‌جمعی** (اسکریپت/SQL) ۱۵ ثانیه کهنه‌اند | درست کار می‌کند: کشِ خوانش‌ها (جستجو + فهرست‌ها) TTL ۱۵ ثانیه‌ای دارد و نوشتنِ دور‌زنِ سرویس آن را نمی‌شکند. دکمهٔ «خالی‌کردنِ کشِ خوانش‌ها» (تنظیمات ← جستجو و مترادف‌ها) همان لحظه تازه‌اش می‌کند؛ یا `search_cache_seconds` را کم کن |
| صفِ پیامک پر می‌شود و هیچ پیامی نمی‌رود، بی‌هیچ خطا | یا سامانه‌ای انتخاب نشده، یا «ارسالِ پیامک» خاموش است، یا کارگر بالا نیست. هر سه در دو خطِ اولِ `npm run worker:once` نوشته می‌شود (پیش‌تر تنها راه، حدس‌زدن بود)؛ برایِ چرخهٔ ماندگار رویِ ماشینِ توسعه: `npm run start:worker` |
| پیامک با پیامِ «شناسهٔ قالب … ندارد» در صندوق مانده | از این نوبت، «خطِ پیامک» در تنظیمات ← پیامک هم کافی است (متنِ آزاد را هر سه سامانه با خطِ تنظیم‌شده می‌پذیرند). بی‌هر دو، رد کردن درست است — متنِ تأییدنشده را خودِ سامانه می‌شکند و اعتبارت را بی‌مصرف می‌سوزاند |
| «آزمونِ ارسال» در پنل سبز است ولی صف خالی نمی‌شود | این دقیقاً همان ناسازگاریِ اصلاح‌شده بود (دکمهٔ پنل متنِ آزاد را می‌فرستاد، کارگر رد می‌کرد). اگر باز دیدی، `npm run worker:once` را اجرا کن و خطِ «پیامک: …» را بخوان؛ دو پاسخِ متعارض باید برایش باگ شمرده شود |
| دو کارگر (تایمر + دستی) پیام را دوباره نمی‌فرستند؟ | نمی‌فرستند: `SKIP LOCKED` + مهلتِ `sending`. سنجیده شد: ۲۴ پیام با ۴ کارگرِ هم‌زمان = ۲۴ ارسال با ۲۴ ارجاعِ یکتا، صفر تکرار |
| لاگِ درخواست‌ها همه `127.0.0.1` است | طبیعی است — نشانیِ فرستاده‌شده فقط وقتی خوانده می‌شود که همتا در `TRUSTED_PROXY_IPS` باشد. رویِ سرور با nginx: `TRUSTED_PROXY_IPS=127.0.0.1` کافی است |
| CI/محلی: «قالبِ سایت کهنه است» | طراحی (storefront.css یا globals.css، فونت، یا تصویرهایِ نمونه) عوض شده ولی قالب بازتولید نشده است. درمان دو خط: `npm run template` و commتِ `docs/qalib.html` با همان تغییر. پرونده را دستی ویرایش نکن — سازنده‌اش `scripts/render-qalib.mjs` است |

## چیزی که این اسکریپت **نمی‌کند**

استقرارِ تولید نیست: کاربرِ سیستمی، systemd، nginx و پشتیبان‌گیری را `deploy/install.sh`
می‌سازد (روی سرورِ لینوکس، با root). این یکی فقط می‌خواهد پروژه روی لپ‌تاپ بالا بیاید.
