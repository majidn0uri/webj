# قراردادِ کاری — ست‌شاپ

> این پرونده «قانونِ کار» رویِ این پروژه است: برایِ انسان و برایِ هوشِ مصنوعی.
> هر کسی (یا هر مدلی) که روی این مخزن کار می‌کند، پیش از نخستین تغییر این را
> بخواند. جایی که قانونی با واقعیتِ پروژه نمی‌خواند، قانون عوض می‌شود — نه
> اینکه دور زده شود.

## ۰. اصلِ کلی

هر تغییر باید مثلِ کارِ یک تیمِ حرفه‌ای باشد: کدِ سالم رویِ شاخه‌یِ اصلی،
تاریخچه‌یِ خوانا، سنجشِ خودکار، و مستندی که از واقعیتِ کد عقب نمانده باشد.
**هیچ کاری «تمام» نیست مگر اینکه دروازه‌ها سبز باشند و گزارش داده شود.**

سه اصل که بر همه‌چیز تقدم دارد:

1. چیزی را که نمی‌دانی حدس نزن — اجرا و تست کن. (اجرایِ واقعی > ادعایِ نظری)
2. در ابهام، بپرس. پیش از تغییرِ بزرگ یا تصمیمِ یک‌طرفه، تأیید بگیر.
3. محدوده‌یِ درخواست را نگه دار. فقط همان که خواسته شده؛ بقیه را به‌عنوان
   «پیشنهاد» فهرست کن، نه در کد.

## ۱. ترتیبِ کار

```
فهمیدن → برنامه‌یِ کوتاه → [پرسیدن اگر مبهم/بزرگ است] → شاخه‌یِ جدا
  → پیاده‌سازی → بازبینیِ diff خودت → دروازه‌ها → مستندات → commit/Push → گزارش
```

- پیش از کارِ بزرگ یا مخرب (تغییرِ معماری، حذفِ دسته‌جمعی، بازنویسی)،
  خلاصه‌یِ «می‌خواهم این‌ها را بکنم» را بگو و صبر کن.
- تغییرِ کوچک و مستقل را بی‌مکث انجام بده، اما ده کار را یک‌جا نریز.
- پیش از commit، خودت `git diff` را بخوان — مثلِ یک بازبینِ بیرونی.
- اگر کار چندمرحله‌ای است، در میانه‌اش هم گزارش بده، نه فقط آخرش.

## ۲. مخزن و گیت

- مخزن: **خصوصی** (`majidn0uri/sell-web`). شاخه‌یِ `main` فقط کدِ سالم و
  سبز می‌گیرد؛ **هرگز مستقیم رویِ `main` commit نزن**.
- کار روی شاخه‌یِ جدا: `feature/<نام-تغییر>`, `fix/<نام>`, `docs/<نام>`.
- commitها کوچک و تک‌منظوره، با قالبِ Conventional Commits:
  `feat:`، `fix:`، `docs:`، `refactor:`، `test:`، `chore:`
- **راز ممنوع است**: توکن، کلید، رمز، گواهی. فقط از متغیرِ محیطی خوانده
  شود. اگر رازی در تاریخچه افتاد، تاریخچه پاک می‌شود (فقط حذفِ فایل کافی
  نیست) و توکن باطل می‌گردد.
- پیش از هر کارِ مخرب (حذف، بازنویسی، force-push)، شاخه‌یِ نجات بساز.

## ۳. کیفیتِ کد

- نام‌گذاریِ روشن؛ بدونِ کوته‌نوشتِ ناروشن.
- هر تابع یک کار انجام بدهد؛ تابعِ بلند (~۳۰ خط به بالا) یعنی بو دارد.
- تکرار ممنوع (DRY) — کدِ تکراری یعنی باگِ آینده.
- عددِ جادویی ممنوع — همه‌چیز ثابتِ نام‌دار یا از تنظیمات.
- کدِ مرده ممنوع — بی‌استفاده را حذف کن، کامنت نکن.
- خطا را قورت نده — یا درست مدیریت کن یا بالا بفرست.
- کمینه‌یِ وابستگی: هر لایه فقط به لایه‌یِ زیرش؛ منطق از رابط جدا بماند.
- کد را طوری بنویس که **«چرا»** یش پیدا باشد، نه اینکه فقط کار کند.

### الزام‌هایِ ویژه‌یِ این پروژه

| الزام | چرا |
|---|---|
| فقط ایران | هیچ منبعِ خارجی (فونت/کتاب‌خانه/تصویر از اینترنت). سایت باید بدونِ اینترنتِ بین‌الملل کامل بالا بیاید |
| راست‌چینِ کامل | رابط فارسی و RTL؛ تاریخ شمسی؛ پول با تومان/ریال |
| بی‌کدنویسی برای فروشنده | هرچه فروشنده نیاز دارد، در پنل باشد |
| مقیاسِ سازمانی | هزاران خریدارِ هم‌زمان، صدها کاربرِ داخلی با وظایفِ جدا |

## ۴. دروازه‌های کیفیت (Definition of Done)

هیچ تغییری پذیرفته نیست مگر **همه‌یِ این‌ها سبز باشند**:

```bash
npm run typecheck        # بررسیِ نوع — ریشه‌یِ مخزن
npm run typecheck:web    # بررسیِ نوع — برنامه‌یِ وب
npm run check            # بازرسیِ پروژه (۶ قانون)
npm run build:web        # ساختِ وب، بی‌خطا
npm test                 # همه‌یِ آزمون‌ها
```

یا یک‌جا:

```bash
npm run verify           # سه دروازه‌یِ نخست (سریع)
```

**بازرس** (`scripts/check-code.mjs`) شش قانون دارد که از الزام‌هایِ واقعیِ
این پروژه آمده‌اند، نه از سبکِ عمومی:

1. هیچ رازی در مخزن (توکن/کلید/رمز)؛
2. هیچ منبعِ خارجی (فونت یا کتاب‌خانه از اینترنت)؛
3. کدِ مرورگر به نشانیِ مطلقِ میزبان وصل نشود؛
4. `console.log`/`debugger` در کتاب‌خانه و کدِ مشتری نماند؛
5. نام‌گذاری و شماره‌گذاریِ مهاجرت‌ها یک‌دست باشد؛
6. کارِ ناتمام (`TODO`/`FIXME`) شمرده شود، پنهان نماند.

تعریفِ «تمام» = همه‌یِ بالا + تستِ دستی/چکِ نهایی + گزارش به کاربر.
اگر جایی قرمز شد، دلیلش را شفاف بگو و درستش کن. اگر وقت/امکان نبود،
**صادقانه بگو چه چیزی تست نشده**.

> نکته‌یِ اجرایی: مجموعه‌یِ آزمونِ کامل روی دستگاهِ کم‌حافظه (۲ گیگابایت)
> با وب/API روشن، کارگرها را می‌کشد. **پیش از اجرایِ `npm test` سرویس‌ها را
> خاموش کن** و بعد دوباره بالا بیاور.

## ۵. اجرایِ خودکار (CI)

`.github/workflows/ci.yml` با هر push و هر Pull Request رویِ سرورِ گیت‌هاب
اجرا می‌شود: بازیابی، بررسیِ نوع، بازرسی، ساخت، و همه‌یِ آزمون‌ها (با سرویسِ
Postgres برای دو پرونده‌ای که به پایگاهِ واقعی نیاز دارند).

رفتارِ لازم پس از هر push: **نتیجه را از API گیت‌هاب بخوان و گزارش بده**
— سبز ✅ یا قرمز ❌ با دلیلِ دقیق. تا سبز نشده، کار تمام نیست.

## ۶. همکاری

- Issue = فهرستِ کار. هر Issue باز خوانده، انجام و بسته شود.
- تغییر در شاخه‌یِ جدا → Pull Request با توضیحِ «چه عوض شد و چرا» (و چه چیزی
  عمداً دست نخورده). تا بازبینی و سبز شدنِ دروازه‌ها، ادغام با `main` ممنوع.
- کامنتِ کاربر روی PR/Issue دستورِ کار است، نه پیشنهاد.

## ۷. وابستگی‌ها

- نسخه‌ها با `package-lock.json` سنجاق شده‌اند؛ آن را commit کن.
- بسته‌یِ پیش‌نمایش (beta/rc) نصب نمی‌شود مگر با اجازه‌یِ صریح.
- پیش از هر تحویل، وابستگی‌ها از نظرِ امنیت بررسی و **گزارش** می‌شوند؛
  ارتقایِ آن‌ها نیازمندِ تأیید و آزمونِ دوباره است.

## ۸. نسخه‌ها و خروجی

- SemVer (`MAJOR.MINOR.PATCH`) و تگ روی هر تحویلِ مهم.
- `CHANGELOG.md` به‌روز شود (تغییراتِ مهم به‌صورت فهرست).
- خروجیِ استقرار با `deploy/install.sh` روی سرور ساخته می‌شود
  (واحدهای systemd در `deploy/systemd/`)؛ پشتیبان با `deploy/backup.sh`.

## ۹. مستندات

- پس از هر تغییر، `README.md` و (اگر لازم شد) این پرونده به‌روز شود.
- هر تصمیمِ مهم و «چرا این‌طور شد» نوشته شود.
- دستورهای اجرا/ساخت/تست دقیقاً همان‌هایی باشند که خودت اجرا کرده‌ای.

مستنداتِ موضوعی در `docs/`:

| پرونده | موضوع |
|---|---|
| `resaneh.md` | بارگذاری و بهینه‌سازیِ تصویر |
| `maliat-moadian.md` | مالیات و سامانه‌یِ مؤدیان |
| `marjoi-garanti.md` | مرجوعی و گارانتی |
| `tarahi-forushgah.md` | نظامِ طراحیِ فروشگاه |
| `moshakhasat/` | اسنادِ مشخصاتِ کارفرما (جریان کار، دستورالعمل، فرم‌ها، فلوچارت) |

## ۱۰. امنیت و حافظه

- هرگز توکن/رمز در مخزن، commit، یا متنِ عمومی نوشته نشود؛ فقط متغیرِ
  محیطی یا پرونده‌یِ بیرون از گیت.
- توکن تک‌منظوره، کم‌دسترسی، و تاریخ‌دار باشد. اگر لو رفت، **فوراً باطل شود**.
- خروجی‌هایِ سنگین (ساخت، کش، تصویرِ آزمایشیِ بزرگ) در مسیرِ ماندگار نماند.

## ۱۱. زبان و قالبِ گزارش

گزارش با فارسی و با همین قالب:

1. **چه انجام شد** — یک خط + جزئیاتِ مهم؛
2. **چه سبز است / چقدر تست شد** — با عددِ واقعی، نه صفت؛
3. **چه مانده یا تست نشده** — صادقانه؛
4. **گامِ بعدی** — پیشنهادِ مشخص.

## ۱۱ب. دو قانونِ همیشگی (تخطی از آن‌ها پذیرفته نیست)

### قانونِ ۱ — پایانِ هر پیام، یعنی بروز شدنِ گیت‌هاب

هر پیامی که در آن پرونده‌ای تغییر کرده، باید به انتشارِ همان تغییرها روی
گیت‌هاب ختم شود. هیچ تغییری «فقط روی دستگاه» نمی‌ماند — مگر آنکه مانعی
بیرونی (مانندِ نبودِ دسترسی) باشد، که در آن صورت باید در گزارش گفته شود.

چرا با این سختی؟ چون در این محیط پوشه‌یِ `.git` میانِ اجراها نمی‌ماند؛
تغییری که منتشر نشود، در بازنشانیِ بعدی از بین می‌رود. انتشار در اینجا یک
تشریفات نیست، تنها راهِ ماندنِ کار است.

ترتیبِ اجرا: شاخه‌یِ جدا → commit → push → Pull Request → خواندنِ نتیجه‌یِ
اجرایِ خودکار → ادغام پس از سبز شدن.

### قانونِ ۲ — پرونده‌یِ پیشرفت، همیشه به‌روز

`docs/pishraft.md` حافظه‌یِ پروژه است و باید در هر بار، به‌روز شود:

- کاری که انجام شد، از بخشِ «آنچه مانده» به «آنچه ساخته شده» می‌رود؛
- کاری که تازه شناخته شد، به «آنچه مانده» افزوده می‌گردد؛
- عددها (آزمون‌ها، مهاجرت‌ها، جدول‌ها) به‌روز می‌شوند.

پیش از آغازِ هر کار آن را بخوان تا بدانی کجا ایستاده‌ای؛ پس از پایان آن را
به‌روز کن تا اجرایِ بعدی بداند.

## ۱۲. بازگردانیِ محیط (مهم در اجرایِ ابری)

در این محیط، آنچه **نمی‌ماند**: پوشه‌یِ `.git`، بسته‌ها (`node_modules`)،
و پایگاهِ داده (برنامه‌هایِ نصب‌شده با apt). آنچه **می‌ماند**: پرونده‌هایِ
زیرِ `/home/user` — یعنی کد، مستندات، و `~/.setshop-github-token`.

پس هر اجرا را با این بازگردانی آغاز کن (دقیقاً همین دستورها آزموده شده‌اند):

```bash
# ۱) پیوندِ مخزنِ محلی به مخزنِ اصلی (چون .git نمی‌ماند)
cd /home/user/set-shop
git init -b main
git remote add origin https://github.com/majidn0uri/sell-web.git
git config credential.helper 'store --file=/tmp/git-creds'
printf 'https://x-access-token:%s@github.com\n' "$(cat ~/.setshop-github-token)" > /tmp/git-creds
chmod 600 /tmp/git-creds
git fetch origin main && git reset --mixed origin/main

# ۲) بسته‌ها
npm ci

# ۳) پایگاهِ واقعی (نسخه‌ی ۱۸ — مخزنِ PGDG)
curl -fsSL https://www.postgresql.org/media/keys/ACCC4CF8.asc \
  | sudo gpg --dearmor -o /usr/share/keyrings/postgresql-archive-keyring.gpg
echo "deb [signed-by=/usr/share/keyrings/postgresql-archive-keyring.gpg] \
  https://apt.postgresql.org/pub/repos/apt trixie-pgdg main" \
  | sudo tee /etc/apt/sources.list.d/pgdg.list >/dev/null
sudo apt-get update -qq && sudo apt-get install -y -qq postgresql-18

# دو دامی که هر بار وقت می‌گیرند:
#   الف) بازنشانی، مجوزِ شاخه‌یِ داده را به ۰۷۵۵ برمی‌گرداند و پستگرس فقط
#        ۰۷۰۰ یا ۰۷۵۰ را می‌پذیرد → را نپذیرفتن با «invalid permissions»
#   ب) اگر شاخه با ساختِ دیگری از پستگرس ساخته شده باشد (پیامِ
#        «without USE_FLOAT8_BYVAL»)، با این بسته باز نمی‌شود؛ پاک و از نو
#        بساز — داده‌اش بذری و بازساختنی است
chmod 700 .data/pg
# شاخه‌یِ ناسازگار: rm -rf .data/pg  (سپس اسکریپت خودش از نو می‌سازد)
mkdir -p /tmp/setshop-pgrun
# نکته‌یِ تعیین‌کننده: PGDATA را **درونِ فضایِ کاری** (/home/user/…) نگذارید.
# فضایِ کاری در پایانِ هر نوبت عکس‌برداری و بازگردانی می‌شود و آن بازگردانی
# سقف دارد؛ یک شاخه‌یِ داده‌یِ زنده (نزدیکِ دو هزار پرونده) در این بازگردانی
# نیمه‌بند می‌شود: پرونده‌هایِ global/ بازنویسی می‌شوند و «pg_filenode.map»
# می‌رود — و پستگرسِ زنده با پیامِ بی‌ربطِ «could not open file…» از کار
# می‌افتد، بی‌هیچ ردی در لاگِ خودش. پیش‌فرضِ اسکریپت همین است: شاخه‌یِ داده در
# /tmp/setshop-pgdata (بیرون از فضایِ کاری)، سوکت هم در /tmp/setshop-pgrun.
PGBIN=/usr/lib/postgresql/18/bin PGDATA=/tmp/setshop-pgdata \
PGRUN=/tmp/setshop-pgrun PGPORT=5433 PGUSER=setshop PGDB=setshop \
  nohup bash scripts/pg-start.sh > /tmp/pg-start.log 2>&1 &

# ۴) طرح و داده
export DB_URL="postgres://setshop@/setshop?host=/tmp/setshop-pgrun&port=5433"
npm run migrate
npx tsx scripts/seed-catalog.ts
ADMIN_MOBILE=09120000000 ADMIN_PASSWORD='SetShop-1405!' npx tsx scripts/create-admin.ts
```

> مخزنِ رویِ گیت‌هاب «منبعِ حقیقت» است، نه مخزنِ محلی. به همین دلیل هر
> تغییرِ تمام‌شده باید منتشر شود — و نه فقط به این دلیل که قرارداد می‌گوید.

> **آزمون‌ها هرگز پایگاهِ توسعه را لمس نمی‌کنند.** `vitest.config.ts` مقدارِ
> `DB_URL` را در محیطِ کارگر بر `memory://` می‌نشاند؛ پس حتی اگر در پوسته
> `export DB_URL=…` باشد، هر پرونده‌یِ آزمون پایگاهِ درون‌حافظه‌یِ خودش را
> می‌سازد و با پایانش پاک می‌شود. چرا اینقدر سخت‌گیرانه؟ چون آلودگیِ پایگاهِ
> توسعه **خرابیِ خاموش** است: هیچ آزمونی قرمز نمی‌شود، اما بیست‌وچند سفارشِ
> آزمایشی در جدولِ سفارش‌ها می‌نشیند و گزارش‌هایِ خروجی عددی بی‌ربط نشان
> می‌دهند.
>
> برایِ اجرایِ کامل (۵۶۰ آزمون در ۳۸ پرونده) وب و API را موقتاً خاموش کن:
> هر کارگر یک نمونه‌یِ پایگاهِ درون‌حافظه می‌سازد و با دو گیگابایت حافظه،
> وب و APIِ زنده جایِ آن را می‌گیرند و کارگرها بی‌سر و صدا کشته می‌شوند.

## ۱۳. جای‌خالی‌هایِ پرشده برای این پروژه

| جای‌خالی | مقدار |
|---|---|
| نامِ پروژه | ست‌شاپ (SetShop) — فروشگاهِ لوازمِ جانبیِ موبایل |
| زبان/پشته | Node 20.20.2 · TypeScript · NestJS 11 (Fastify) · Next 15 · PostgreSQL 18 · Vitest |
| مسیرِ پروژه | `/home/user/set-shop` (monorepo با npm workspaces) |
| دستورِ بیلد | `npm run build:web` |
| دستورِ تست | `npm test` (با وب/API خاموش) |
| بازرس | `node scripts/check-code.mjs` (۶ قانون) |
| مهاجرت | `npm run migrate` |
| استقرار | `deploy/install.sh` + `deploy/systemd/` |
| مرجعِ دستورها | `README.md` و این پرونده |
