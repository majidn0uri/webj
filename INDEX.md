# راهنمایِ این کپی — ست‌شاپ (SET)

> این پوشه یک **کپیِ کامل از پرونده‌هایِ اصلیِ پروژه + همهٔ مستندات** است.
> بدونِ `.git` (تاریخچه)، `node_modules` و دادهٔ پایگاه — یعنی هر چیزی که برایِ
> خواندن، بررسی و اجرایِ پروژه لازم است، و نه یک بایت اضافه.

| | |
|---|---|
| سرچشمه | مخزنِ `majidn0uri/sell-web` رویِ گیت‌هاب، شاخهٔ `main` |
| کامیت | `8717315` — Merge pull request #42 (بازگشتِ کامل به قالبِ پیشین) |
| هَشِ درخت | `6f8eb6631df96c91faa3d312b2ae78c33a9792fc` — دقیقاً برابرِ حالتِ پیش از بازسازیِ قالب |
| تاریخِ کپی | ۱۴۰۵/۰۶/۳۱ (۲۰۲۶/۰۹/۲۲) |
| اندازه | ۱۲ مگابایت، **۶۴۰ پرونده** (برابرِ شمارِ پرونده‌هایِ tracked در گیت) |

## شمارشِ واقعیِ محتویات (سنجیده شد)

| بخش | مقدار |
|---|---|
| TypeScript / TSX | **۸۳٬۱۵۵ خط** |
| SQL (مهاجرت‌هایِ پایگاه) | **۴٬۱۸۵ خط** در **۴۹ مهاجرت** |
| CSS | **۳٬۹۰۵ خط** (۴ پرونده: `globals`, `storefront`, `account`, `premium`) |
| پروندهٔ آزمون | **۶۲** (مجموعاً **۸۷۵ آزمون**، همه سبز) |

## از کجا شروع کنم؟

1. **`README.md`** — پروژه چیست، چه فازهایی ساخته شده، عددهایِ واقعی.
2. **`SETUP-LOCAL.md`** — اجرایِ کامل رویِ کامپیوترِ خودت (نصب، پایگاه، API، وب، کارگر، عیب‌یابی).
3. **`AGENTS.md`** — قراردادِ کاریِ پروژه: قانونِ شاخه، commit، دروازه‌هایِ کیفیت، ممنوعیتِ منابعِ خارجی.
4. **`docs/pishraft.md`** — حافظهٔ پروژه: چه ساخته شده و چه مانده.
5. **`docs/qalib.html`** — مرجعِ بصریِ کاملِ سایت (۱٫۴۷ مگابایت، خودکفا: فونت و تصویر داخلش) — با هر مرورگری بازش کن.

## نقشهٔ پوشه‌ها

```
apps/api/          ← بک‌اندِ NestJS: مسیرها، RBAC، PDF، صفِ پیامک
apps/web/          ← فروشگاه + پنلِ مدیریت (Next.js 15 / React 19 / TypeScript)
   src/app/           ← صفحه‌ها + چهار پروندهٔ CSS قالب
   src/components/    ← اجزایِ ویترین و پنل
packages/          ← ۲۵ بستهٔ دامنه: accounting, cart, catalog, commerce, coupons,
                     crm, db, exports, inventory, media, observability, orders,
                     payments, pos, procurement, rate-limit, rbac, reports, returns,
                     reviews, shared-kernel, shopper, tax, auth, content
   db/migrations/     ← ۴۹ مهاجرتِ SQL
scripts/           ← ابزارها: بازتولیدِ قالب، بررسیِ کد، ممیزیِ بصری، seed، کارگر
docs/              ← همهٔ مستنداتِ فارسی (جدولِ پایین)
   qalib.html         ← مرجعِ بصریِ سایت
   moshakhasat/       ← اسنادِ مشخصاتِ کارفرما (فلوچارت، فرم‌ها، پیش‌نمایش)
   drill/             ← تصویرهایِ صفحات (مرجوعی، تسویه، رسانه و…)
deploy/            ← نصب رویِ سرور: nginx، systemd، پشتیبان‌گیری، بازیابی
قالب نمونه/         ← مرجعِ طراحیِ کارفرما (setshop-premium.html + فونت‌ها + تصویرها)
                     ⚠️ فقط مرجع است — نباید تغییر کند و کدش واردِ برنامه نمی‌شود
setup.sh / start.sh / start.bat / *.ps1  ← شروعِ سریع (لینوکس/مک و ویندوز)
```

## مستندات (۲۹ سند)

### ریشهٔ پروژه

| پرونده | چیست |
|---|---|
| `README.md` | معرفیِ پروژه، فازهایِ اجراشده، عددهایِ سنجیده‌شده |
| `SETUP-LOCAL.md` | راهنمایِ اجرایِ محلی + جدولِ عیب‌یابی |
| `AGENTS.md` | قراردادِ کاری: شاخه، commit، دروازه‌هایِ کیفیت، بایدها و نبایدها |
| `CHANGELOG.md` | ثبتِ تغییرها (قالبِ Keep a Changelog) |
| `AUDIT-FINAL.md` | بررسیِ نهایی: چه واقعی است، چه نمایشی، چه ناقص |
| `IMPLEMENTATION-AUDIT.md` | مقایسهٔ فلوچارتِ واقعی با پیاده‌سازی |
| `FLOW-AUDIT.md` · `FLOWCHARTS-HONEST.md` · `FLOWCHARTS-DETAILED.md` · `BUSINESS-FLOWCHARTS.md` · `COMPREHENSIVE-FLOWCHART.md` | فلوچارت‌هایِ جریانِ کارِ فروشگاه (از کلی تا جزئی) |

### `docs/`

| پرونده | چیست |
|---|---|
| `pishraft.md` | **حافظهٔ پروژه** — چه ساخته شده و چه مانده |
| `tarahi-forushgah.md` | طراحیِ بصریِ فروشگاه: مبنا، رنگ، چیدمان |
| `qalib.html` | مرجعِ بصریِ کاملِ سایت (خودکفا) |
| `nashast-va-dastresi.md` | نشست و دسترسی در پنل (RBAC) |
| `pardakht-iran.md` | پرداختِ ایرانی: درگاه‌ها و جریانِ تسویه |
| `hesab-moshtari.md` | حسابِ کاربریِ مشتری: طراحی و امنیت |
| `moshtariyan.md` · `crm` در پنل | مدیریتِ مشتریان |
| `marjoi-garanti.md` | مرجوعی و گارانتی |
| `tamin-va-kharid.md` | تأمین و خرید |
| `maliat-moadian.md` | مالیاتِ ایران: ارزش‌افزوده و مودیان |
| `gozaresh-modiriyati.md` | گزارش‌هایِ مدیریتی: سودِ ناخالص، نقطهٔ سربه‌سر |
| `bar-va-tavan.md` | بار و توان: «هزاران خریدارِ هم‌زمان» |
| `mahar-bar-va-didebani.md` | مهارِ بار و دیده‌بانی (Rate limit + Observability) |
| `resaneh.md` | رسانه: بارگذاری و بهینه‌سازیِ تصویر |
| `tanzimat-va-payamak.md` | تنظیماتِ فروشگاه و مرکزِ پیامک |
| `momayezi-basari.md` | ممیزیِ بصری: اندازه‌گیری به‌جایِ حدس |
| `shokaf-moshakhasat.md` | سنجشِ ساخته‌شده با اسنادِ مشخصات |
| `premium-redesign.md` | گزارشِ بازطراحیِ فروشگاه و پنل (۲۰۲۶/۰۹/۲۲) |
| `admin-stabilization.md` | تکمیلِ زیرساختِ پنل پس از بازطراحی |

## اجرایِ پروژه

```bash
npm ci                 # نصبِ وابستگی‌ها (نسخه‌ها قفل‌شده در package-lock.json)
npm run verify         # دروازه‌هایِ کیفیت: typecheck (×۳) + check
npm run build:web      # ساختِ فروشگاه و پنل
npm test               # ۸۷۵ آزمون (۶۲ پرونده)
npm run template       # بازتولیدِ docs/qalib.html از CSS زندهٔ سایت
```

اجرایِ کامل با پایگاه و API: `SETUP-LOCAL.md` را بخوان — گام‌به‌گام نوشته شده.

## چه چیزی در این کپی **نیست**

- `.git/` — تاریخچهٔ کاملِ تغییرها (رویِ گیت‌هاب هست: `majidn0uri/sell-web`)
- `node_modules/` — با `npm ci` ساخته می‌شود
- `.data/` و `var/` — دادهٔ پایگاهِ محلی (با `npm run db:reset` از نو ساخته می‌شود)
- `.env` و هر فایلِ راز — عمداً کپی نشد (هیچ رازی در مخزن نیست)

## دو نکتهٔ مهم

1. **`قالب نمونه/` مرجعِ طراحی است، نه کدِ برنامه.** زبانِ پیاده‌سازیِ پروژه
   TypeScript + React + Next.js است و باید همین بماند؛ از نمونه فقط چیدمان،
   اندازه‌ها، رنگ و شکلِ اجزا برداشته می‌شود.
2. **قالبِ کنونی، همان قالبِ پایدارِ پیشین است.** تلاشِ بازسازیِ قالب
   (PR #40 و #41) با PR #42 کامل برگردانده شد، چون شش بخشِ امکاناتِ واقعی از
   `docs/qalib.html` افتاده بود: پرفروش‌ها، فیلترها، مقایسهٔ ۳ کالا،
   دیده‌شده‌هایِ اخیر، تاریخچهٔ قیمت و اجزایِ پنل. آن کار در تاریخچهٔ گیت‌هاب
   سالم مانده (`319fcf5` و `add38a6`) و در صورتِ نیاز گزینشی برمی‌گردد.
