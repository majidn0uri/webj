# بررسی نهایی — واقعی، نمایشی، ناقص، جامانده

> هر مرحله با کد واقعی بررسی شده. سه وضعیت:
> ✅ واقعی و کاربردی · ⚠️ نمایشی/سطحی · ❌ جامانده/حفره

---

## فلوچارت ۱: فروش آنلاین

### ۱.۱ جستجو و مرور محصولات — ✅ واقعی

**چرا واقعی؟**
- موتور جستجوی تمام‌متنی با `search_key` (packages/catalog/src/index.ts:162)
- مترادف‌ها: `expandSearchQuery()` با بارگذاری از `search_synonyms` (index.ts:618)
- کش: `searchCachePolicy()` + `cacheGet()` — سیاست از پنل مدیر (index.ts:586)
- فیلتر: برند، نوع، رنگ، قیمت، موجودی، سازگاری گوشی (structural conditions)
- مرتب‌سازی: جدیدترین، ارزان‌ترین، پرفروش‌ترین (14 روز اخیر)
- SQL واقعی با `EXISTS`, `JOIN`, `ORDER BY score` — نه hardcoded

### ۱.۲ صفحه محصول — ✅ واقعی

**چرا واقعی؟**
- `productBySlug()` — خواندن کامل از DB
- تصاویر: `product_images` با role (اصلی، کارت، پلیس‌بک)
- مشخصات فنی: `product_specs` — key-value
- سازگاری: `product_compatibility` → `device_models`
- تنوعات: `product_variants` — SKU, قیمت, ویژگی‌ها

### ۱.۳ افزودن به سبد — ✅ واقعی

**چرا واقعی؟**
- سبد در سمت سرور (`carts` + `cart_items`)
- کوکی فقط `cart_id` را نگه می‌دارد
- قیمت از DB می‌آید (نه از مرورگر — امنیت)
- موجودی بررسی می‌شود (`available > 0`)

### ۱.۴ سبد خرید — ✅ واقعی

**چرا واقعی؟**
- `getCart()` — خواندن آیتم‌ها + محاسبه با `calcLine()` + `sumLines()`
- `calcLine()` در shared-kernel: `net = gross - discount`, `tax = percentOf(net, taxBasisPoints)`, `total = net + tax`
- تخفیف پیش از مالیات محاسبه می‌شود (درست طبق قانون ایران)

### ۱.۵ اعمال کد تخفیف — ✅ واقعی

**چرا واقعی؟**
- `validateCoupon()`: بررسی کد، فعال بودن، تاریخ، تعداد، مشتری، حداقل مبلغ
- `applyCouponRecord()`: توزیع بین ردیف‌ها بر اساس `categoryId` + فرزندان
- `redeemCoupon()`: ثبت مصرف در `coupon_redemptions` — در همان تراکنش سفارش
- `releaseCoupon()`: آزادسازی پس از لغو — مشتری کوپنش را از دست نمی‌دهد

### ۱.۶ ورود / ثبت‌نام — ✅ واقعی

**چرا واقعی؟**
- OTP 6 رقمی + هش (`otpHash`) + TTL 120 ثانیه
- محدودیت: `OTP_MAX_ATTEMPTS = 5` + بررسی `isRecentlyConsumedCode()`
- ثبت‌نام: `INSERT INTO customers` + `mobile_verified_at`
- اتصال مهمان: `claimGuestOrders()` — سفارشات قبلی وصل می‌شود
- پیامک `welcome` ✅ وصل شد
- پیامک `otp_login` ✅ از قبل وصل بود

### ۱.۷ Checkout — ✅ واقعی

**چرا واقعی؟**
- `createOrder()` — 11 مرحله اتمیک:
  1. Idempotency key (جلوگیری از تکرار)
  2. شماره سفارش اتمیک (`number_sequences`)
  3. عکس‌برداری قیمت (از DB)
  4. محاسبه مالیات (از تنظیمات `vat_rate_percent`)
  5. اعمال کوپن
  6. رزرو موجودی (`reserveStock` → `UPDATE stock_items SET reserved += quantity WHERE on_hand - reserved >= quantity`)
  7. درج سفارش + ردیف‌ها
  8. تاریخچه وضعیت
  9. انتشار رویداد
  10. مصرف کوپن
  11. تنظیم انقضای رزرو (15 دقیقه)
- بررسی سقف اعتبار همکار ✅ (cart.controller.ts)

**⚠️ نمایشی/ناقص:**
- ⚠️ انتخاب روش ارسال نیست — `shippingRial` hardcoded یا از تنظیمات
- ⚠️ نمایش ETA نیست (`eta_days` در `shipping_tariffs` موجود ولی نمایش داده نمی‌شود)
- ⚠️ آدرس فقط متن آزاد — انتخاب استان/شهر نیست

### ۱.۸ پرداخت — ✅ واقعی

**چرا واقعی؟**
- ۳ درگاه واقعی: sandbox, zarinpal, idpay
- `PaymentService.start()` → `gateway.start()` → لینک بانک
- `PaymentService.verify()` → `gateway.verify()` → تأیید نزد بانک
- قفل ردیف (`FOR UPDATE`) — جلوگیری از race condition

### ۱.۹ تأیید پرداخت — ✅ واقعی

**چرا واقعی؟**
- `confirmPaymentOn()` در یک تراکنش اتمیک:
  1. بررسی وضعیت + مبلغ
  2. `confirmStock()` → `on_hand -= quantity`, `reserved -= quantity`, `stock_movement`
  3. `postSaleOn()` → سند حسابداری:
     - بدهکار بانک/صندوق/اسناد دریافتنی (بر اساس روش پرداخت)
     - بستانکار درآمد فروش + ارزش افزوده
     - بدهکار بهای تمام‌شده / بستانکار موجودی کالا
  4. ثبت payment
  5. تغییر وضعیت + تاریخچه + رویداد
- SMS: `order_paid` + `order_confirmed` ✅

### ۱.۱۰ بسته‌بندی — ✅ واقعی

**چرا واقعی؟**
- `PackingService.pack()` — بررسی موجودی + ثبت کسری
- `shortages = lines.filter(line => line.available < line.quantity)`
- `packing_complete = false` اگر کسری باشد
- تاریخچه + لاگ

### ۱.۱۱ تأیید سفارش — ✅ واقعی

**چرا واقعی؟**
- Endpoint: `POST /admin/orders/{id}/confirm` ✅
- UI: دکمه «تأیید سفارش» در `order-actions.tsx` ✅
- `assertTransition(order.status, 'confirmed')` — اعتبارسنجی انتقال

### ۱.۱۲ ارسال — ✅ واقعی

**چرا واقعی؟**
- `createShipment()` — بررسی کد رهگیری (حداقل 6 کاراکتر)
- شرط: فقط از وضعیت `packing` (یا `confirmed` اگر `packing_required_before_ship` غیرفعال)
- `INSERT INTO shipments` + تغییر وضعیت
- SMS: `order_shipped` ✅
- برچسب PDF ✅

### ۱.۱۳ تحویل — ✅ واقعی

**چرا واقعی؟**
- `markDelivered()` — آپدیت shipment + order + تاریخچه
- SMS: `order_delivered` ✅

### ۱.۱۴ لغو — ✅ واقعی

**چرا واقعی؟**
- `OrderService.cancel()` — آزادسازی رزرو + برگشت کوپن + تاریخچه + رویداد
- SMS: `order_cancelled` ✅

### ۱.۱۵ مرجوعی — ✅ واقعی

**چرا واقعی؟**
- `requestReturn()` → `decideReturn()` → `completeReturn()`
- بازگشت موجودی: `on_hand += quantity` + `stock_movement`
- سند حسابداری معکوس: swap debit/credit سند اصلی
- بازپرداخت: bank/cash/credit/check_void
- PDF فاکتور مرجوعی ✅
- SMS تأیید/رد ✅

---

## فلوچارت ۲: فروش همکار

### ۲.۱ قیمت همکاری — ✅ واقعی

**چرا واقعی؟**
- SQL: `COALESCE(v.partner_price_rial, v.price_rial)` در search + list
- `isPartner` از توکن → `SELECT is_partner FROM customers`
- `partner_price_rial` یا `partner_price_percent` — هر دو پشتیبانی

### ۲.۲ سقف اعتبار — ✅ واقعی

**چرا واقعی؟**
- در `cart.controller.ts` → `checkout()`: بررسی بدهی معوق در برابر `credit_rial`
- `SELECT SUM(total_rial) FROM orders WHERE customer_id = $1 AND status NOT IN ('paid','delivered','cancelled','refunded')`
- اگر `unpaid >= credit` → خطای فارسی

**⚠️ نمایشی/ناقص:**
- ⚠️ سقف چک (`checkCeilingToman`) در checkout بررسی نمی‌شود — فقط در `registerCheck()`
- ⚠️ پرداخت با چک در checkout آنلاین نیست (فقط در POS)
- ⚠️ خرید نسیه (بدون پرداخت آنلاین) در checkout پیاده‌سازی نشده

---

## فلوچارت ۳: فروش حضوری POS

### ۳.۱ گشایش شیفت — ✅ واقعی

### ۳.۲ فروش + پرداخت فوری — ✅ واقعی

**چرا واقعی؟**
- `PosService.sell()` → `OrderService.createOrder()` + `confirmPayment()` فوری
- resolve customerId از customerMobile ✅
- 5 روش پرداخت: cash/card/card_to_card/cheque/cod
- سند حسابداری: نقد→1100, کارت→1200, چک→1400
- SMS: `order_paid` + `order_confirmed` ✅

### ۳.۳ چاپ فاکتور — ✅ واقعی

**چرا واقعی؟**
- PDF: `GET /admin/exports/orders/{id}/invoice.pdf` — `renderInvoicePdf()`
- دکمه چاپ در UI: `pos-terminal.tsx` — نمایش بعد از هر فروش ✅

### ۳.۴ بستن شیفت — ✅ واقعی

**چرا واقعی؟**
- `PosService.closeShift()` — محاسبه: `expected = opening + cash_sales + net_movements`
- `difference = counted - expected`
- ثبت مغایرت

---

## فلوچارت ۴: تأمین کالا

### ۴.۱ تعریف محصول — ✅ واقعی

### ۴.۲ تعریف تأمین‌کننده — ✅ واقعی

### ۴.۳ درخواست خرید — ✅ واقعی

### ۴.۴ رسید خرید + ورود به انبار — ✅ واقعی

**چرا واقعی؟**
- `receiveGoods()` — تراکنش اتمیک:
  1. درج رسید + ردیف‌ها
  2. ورود کالای سالم به انبار (`on_hand += sellable`)
  3. `stock_movement` برای سالم + ضایعات
  4. `updateWeightedAverage()` — WAC
  5. مغایرت‌ها: کسری، مازاد، معیوب، تفاوت قیمت
- SMS: `product_back` به مشتریان منتظر ✅

### ۴.۵ فاکتور خرید — ✅ واقعی

**چرا واقعی؟**
- `postPurchaseInvoice()` — اعتبارسنجی هویت ایرانی (شناسه ملی + کد اقتصادی)
- قید یکتا: `supplier_national_id + supplier_invoice_no`
- سند حسابداری خودکار: موجودی + ارزش افزوده خرید / حساب پرداختنی
- PDF فاکتور خرید ✅

**⚠️ نمایشی/ناقص:**
- ⚠️ اتصال فاکتور به سفارش خرید ضعیف — `purchase_invoice_id` در رسید هست ولی مدیر نمی‌بیند کدام فاکتور مربوط به کدام سفارش
- ⚠️ صدور سفارش خرید (Purchase Order) جدا نیست — مستقیم از درخواست به رسید

---

## فلوچارت ۵: حسابداری

### ۵.۱ سند دستی — ✅ واقعی

**چرا واقعی؟**
- `postJournal()` — شماره اتمیک + بررسی تراز (`totalDebit !== totalCredit` → خطا)
- قید پایگاه: `journal_line_side_chk` — هر ردیف یا بدهکار یا بستانکار

### ۵.۲ برگشت سند — ✅ واقعی

### ۵.۳ ترازنامه آزمایشی — ✅ واقعی

### ۵.۴ صورت سود و زیان — ✅ واقعی

### ۵.۵ ترازنامه — ✅ واقعی

**⚠️ نمایشی/ناقص:**
- ⚠️ مغایرت بانکی — جدول `bank_reconciliation` نیست، تطبیق دستی است

---

## فلوچارت ۶: مالیات

### ۶.۱ گواهی مالیاتی — ✅ واقعی

### ۶.۲ ارسال به مؤدیان — ✅ واقعی

**چرا واقعی؟**
- `setshop-tax.timer` — systemd timer واقعی
- `scripts/tax-worker.ts` — آزادسازی + ارسال دسته‌ای + استعلام
- `moadian_auto_send` — ارسال خودکار پس از پرداخت
- SSTID در مدل محصول

**⚠️ نمایشی/ناقص:**
- ⚠️ دانلود صورتحساب‌های دریافتی از مؤدیان نیست (فقط ارسال)

---

## فلوچارت ۷: پیامک

### ۷.۱ صف + ارسال — ✅ واقعی

**چرا واقعی؟**
- `sms_outbox` — صف واقعی
- ۳ ارسال‌کننده: `kavenegar` (با lookup)، `melipayamak`، `farazsms`
- `IRAN_SERVICE_HOSTS` — فقط ایران (امنیت)
- `setshop-postsale.timer` — systemd timer واقعی
- تلاش مجدد با فاصله فزاینده
- وضعیت‌ها: pending → sending → sent/failed/dead

**⚠️ نمایشی/ناقص:**
- ⚠️ نمایش جزئیات هر پیامک (متن، خطا) در پنل نیست — فقط شمارش وضعیت‌ها

---

## فلوچارت ۸: انبار

### ۸.۱ شمارش فیزیکی — ✅ واقعی

**چرا واقعی؟**
- ۴ endpoint: ایجاد، ثبت، بستن، لیست
- عکس‌برداری از `on_hand` هنگام ایجاد
- محاسبه `diff_qty` (ستون محاسبه‌شده در DB)
- نمایش مغایرت‌ها هنگام بستن

### ۸.۲ انتقال بین انبارها — ✅ واقعی

**چرا واقعی؟**
- تراکنش اتمیک: `FOR UPDATE` روی مبدأ + کاهش + افزایش
- `stock_movement` برای هر دو طرف
- بررسی موجودی کافی

**⚠️ نمایشی/ناقص:**
- ⚠️ UI برای شمارش و انتقال ساخته نشده — فقط API

---

## فلوچارت ۹: مشتریان

### ۹.۱ لیست مشتریان — ✅ واقعی

### ۹.۲ ثبت چک — ✅ واقعی

**چرا واقعی؟**
- `registerCheck()` — کنترل سقف (`checkCeiling`)
- بررسی چک‌های برگشتی → اگر دارد → سقف صفر
- وضعیت‌ها: in_circulation → deposited → settled/bounced/transferred/void
- `check_status_logs` — لاگ هر تغییر

### ۹.۳ تغییر وضعیت چک — ✅ واقعی

**چرا واقعی؟**
- `changeCheckStatus()` — اعتبارسنجی انتقال مجاز
- SMS: `check_bounced` ✅

**⚠️ نمایشی/ناقص:**
- ⚠️ UI مدیریت چک در پنل ساده است
- ⚠️ آمار کل مشتری (مجموع خرید، تعداد، میانگین) نیست

---

## فلوچارت ۱۰: کوپن

### ۱۰.۱ ایجاد کوپن — ✅ واقعی

### ۱۰.۲ اعتبارسنجی — ✅ واقعی

**⚠️ نمایشی/ناقص:**
- ⚠️ تخفیف حجمی (خرید بیشتر = تخفیف بیشتر) نیست

---

## فلوچارت ۱۱: امنیت

### ۱۱.۱ ورود — ✅ واقعی

### ۱۱.۲ بررسی دسترسی — ✅ واقعی

**⚠️ نمایشی/ناقص:**
- ⚠️ rate limiting فقط روی ورود/ثبت‌نام — نه روی همه APIها
- ⚠️ لاگ فعالیت‌ها فقط برای بعضی عملیات (نه همه)

---

# حفره‌های واقعی — چیزهایی که در کسب‌وکار واقعی باید باشد ولی نیست

## 🔴 بحرانی (حفره بزرگ)

### ۱. صفحه checkout واقعی نیست
**واقعیت:** مشتری فقط آدرس می‌نویسد. انتخاب روش ارسال، نمایش هزینه ارسال بر اساس شهر، نمایش ETA — هیچ‌کدام نیست.
**باید باشد:** انتخاب پست پیشتاز/سفارشی/تیپاکس + هزینه متفاوت + زمان تخمینی

### ۲. خرید نسیه برای همکار نیست
**واقعیت:** همکار فقط می‌تواند آنلاین پرداخت کند. `credit_rial` فقط سقف را کنترل می‌کند ولی «پرداخت نقدی حذف و به حساب بدهکار منظور شود» نیست.
**باید باشد:** گزینه «خرید نسیه» در checkout همکار → سند حسابداری: بدهکار حساب دریافتنی

### ۳. پرداخت با چک در checkout آنلاین نیست
**واقعیت:** `cheque` فقط در POS هست. همکار آنلاین نمی‌تواند با چک پرداخت کند.
**باید باشد:** فرم ثبت اطلاعات چک (شماره، بانک، تاریخ سررسید) در checkout

### ۴. فاکتور فروش آنلاین به مشتری داده نمی‌شود
**واقعیت:** PDF فاکتور فقط در پنل مدیر هست. مشتری پس از پرداخت هیچ فاکتوری دریافت نمی‌کند.
**باید باشد:** لینک دانلود فاکتور در صفحه نتیجه پرداخت + ایمیل فاکتور

### ۵. قیمت تمام‌شده در رسید خرید ثبت نمی‌شود
**واقعیت:** `receiveGoods()` WAC را آپدیت می‌کند ولی `inventory_valuation` جدا از `stock_items` هست. `postPurchaseInvoice()` هم `inventory_valuation` را آپدیت می‌کند. این دو ممکن است از هم جدا بیفتند.
**باید باشد:** یک منبع حقیقت برای بهای تمام‌شده

## ⚠️ مهم (حفره متوسط)

### ۶. مغایرت بانکی نیست
**واقعیت:** جدول `bank_reconciliation` نیست. تطبیق تراکنش‌های بانکی با اسناد حسابداری دستی است.

### ۷. UI شمارش انبار و انتقال نیست
**واقعیت:** API ساخته شده ولی صفحه‌ای در پنل مدیر برای شمارش فیزیکی یا انتقال بین انبارها نیست.

### ۸. فاکتور خرید به تأمین‌کننده ارسال نمی‌شود
**واقعیت:** سفارش خرید فقط در DB ثبت می‌شود. ارسال ایمیل/چاپ/ارسال به تأمین‌کننده نیست.

### ۹. گزارش سن بدهی (Aging Report) نیست
**واقعیت:** `debtors()` گزارش بدهکاران دارد ولی تفکیک بر اساس سن بدهی (30/60/90 روز) نیست.

### ۱۰. تخفیف حجمی نیست
**واقعیت:** تخفیف فقط روی محصول یا کوپن. «خرید ۳ عدد = ۱۰٪ تخفیف» نیست.

### ۱۱. پیگیری سفارش توسط مشتری ضعیف است
**واقعیت:** صفحه `/account/orders` لیست سفارشات را نشان می‌دهد ولی جزئیات وضعیت (کد رهگیری، نام شرکت حمل، لینک پیگیری) نیست.

### ۱۲. پیامک OTP خودش کد می‌فرستد ولی از صف پیامک استفاده نمی‌کند
**واقعیت:** `requestOtp()` → `enqueueSms()` با قالب `otp_login`. این وصل هست ولی اگر صف پیامک خراب باشد، OTP هم خراب می‌شود — باید fallback داشته باشد.

## 🔵 مطلوب (بهبود)

### ۱۳. مقایسه قیمت تأمین‌کنندگان
**واقعیت:** هر بار فقط یک تأمین‌کننده. مقایسه تاریخچه قیمت تأمین‌کنندگان مختلف نیست.

### ۱۴. پیش‌بینی تقاضا
**واقعیت:** `admin-inventory-forecast.controller.ts` هست ولی ساده است — فقط بر اساس فروش گذشته.

### ۱۵. چندزبانه بودن
**واقعیت:** همه فارسی. پشتیبانی از زبان دیگر نیست (برای مشتریان غیرفارسی‌زبان).

### ۱۶. اپلیکیشن موبایل
**واقعیت:** فقط وب. اپلیکیشن نیست.

---

# خلاصه نهایی

| وضعیت | تعداد |
|-------|-------|
| ✅ واقعی و کاربردی | **۳۸ مرحله** |
| ⚠️ نمایشی/سطحی | **۱۲ مورد** |
| ❌ حفره واقعی | **۱۶ مورد** (4 بحرانی + 7 مهم + 5 مطلوب) |

### ۴ حفره بحرانی که باید فوری رفع شوند:
1. **checkout واقعی** — انتخاب روش ارسال + هزینه + ETA
2. **خرید نسیه همکار** — گزینه پرداخت اعتباری در checkout
3. **فاکتور فروش به مشتری** — لینک دانلود پس از پرداخت
4. **پرداخت با چک در checkout** — فرم اطلاعات چک