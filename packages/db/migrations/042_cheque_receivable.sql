-- =====================================================================
-- حسابِ اسناد دریافتنی — برای پرداخت با چک در POS
-- =====================================================================

-- حسابِ «اسناد دریافتنی»: چک‌های دریافتی از مشتریان
INSERT INTO accounts (code, name, type) VALUES
  ('1400', 'اسناد دریافتنی', 'asset')
ON CONFLICT (code) DO NOTHING;