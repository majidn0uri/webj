# تغییرات

همه‌یِ تغییرهایِ مهم این پروژه اینجا ثبت می‌شود. قالب بر پایه‌یِ
[Keep a Changelog](https://keepachangelog.com/fa/1.1.0/) و نسخه‌گذاری بر
پایه‌یِ SemVer.

## [Unreleased]

### رفع موانع مدیریت — ۲۰۲۶/۰۹/۲۲

- رفع ۳۷ خطای نوع ریشه و دو خطای اسکریپت‌ها، نگهبان انبار/حسابداری، PDF خرید/مرجوعی و قراردادهای CRM؛ بدون خاموش‌کردن دروازه‌ها.
- شماره تکراری مهاجرت ارسال/چک به ۰۴۹ منتقل شد؛ نام تاریخی نگه داشته می‌شود و روی نصب قدیمی مجوزها دوباره اعطا نمی‌شوند.
- شناسه صیاد در checkout دریافت می‌شود؛ مرسوله مشتری با شماره سفارش و مالک آن خوانده می‌شود.
- ۱۵ آزمون رگرسیون تازه؛ مجموعه کامل **۸۷۵ آزمون در ۶۲ پرونده موفق**. گزارش `docs/admin-stabilization.md`.


### بازطراحی رابط — ۲۰۲۶/۰۹/۲۲

- الگوگیری بصری از قالب نمونه بدون تغییر پشته TypeScript/React/Next.js؛ هدر، هیرو، دسته‌ها، کارت‌ها و فوتر تازه.
- پوسته مدیریت با ناوبری گروه‌بندی‌شده، جستجوی بخش‌های مجاز، منوی موبایل، سربرگ، آمار واقعی و میانبرهای پیشخوان؛ جدول‌ها، فرم‌ها و وضعیت‌ها هماهنگ شدند.
- هفت آزمون جدید ناوبری و اسکریپت بررسی ۲۴ حالت مسیر/عرض در مرورگر؛ سازنده قالب خودکفا شامل لایه طراحی جدید شد.
- نتیجه و مانع‌های قبلی کیفیت در `docs/premium-redesign.md` ثبت شدند؛ انتشار آماده ادغام در main اعلام نشده است.


### افزوده‌شده

- **تاریخچهٔ قیمت** (`product_price_log` + `price-history.ts` + `GET /products/:slug/price-history` +
  نمایش در صفحه‌یِ کالا): هر تغییرِ قیمت ثبت می‌شود و خریدار می‌بیند قیمتِ
  فعلی واقعی است یا فروشنده قیمت را بالا برد و بعد تخفیف زده. این ویژگی
  چیزی است که دیجی‌کالا ندارد — و بزرگ‌ترین سیگنالِ اعتماد در فروشگاهِ
  آنلاین است. ۵ آزمونِ تازه (سیدِ آغازین، ثبت، فشرده‌سازیِ روزانه، ناموجود،
  غیرفعال). مهاجرتِ ۰۴۰ (`product_price_log`). `recordPriceChange` در
  `createProduct` و `updateProduct` قلاب شده.

- **بزرگ‌نماییِ تصویرِ کالا — نورباکسِ تمام‌صفحه** (`image-lightbox.tsx` +
  CSS بخشِ ۱۱ `storefront.css`): ضربه رویِ تصویرِ صفحه‌یِ کالا → نورباکس
  با جابه‌جاییِ افقی (دکمه/لمس/کلید)، نوارِ بندانگشتی، شمارنده، بستن با
  ESC/ضربه رویِ پس‌زمینه. pinch-to-zoom بومی (مرورگر خودش مدیریت می‌کند).
  پیشرفتِ فزاینده: بدونِ JS، تصویرِ اصلی همچنان نمایش داده می‌شود.

- **دیده‌شده‌هایِ اخیر** (`recently-viewed.tsx` +
  CSS بخشِ ۱۲ `storefront.css`): نوارِ افقی در صفحه‌یِ کالا که تا ۸
  کالایِ بازدیدشده را (تصویر + نام) نشان می‌دهد. `localStorage` — بدونِ
  سرور، بدونِ کوکی، بدونِ وضعیت. خریدارِ بی‌اکانت هم فهرستش محفوظ است.
  قیمت/موجودی عمداً نمی‌آید (ممکن است قدیمی باشد و گمراه کند).

- **مقایسه‌یِ کالا — دو تا چهار کالا در یک نگاه** (`packages/catalog/src/compare.ts`
  + `GET /catalog/compare` + برگه‌یِ `/compare`): بزرگ‌ترین موردِ باقی‌مانده‌یِ
  «به تعویق افتاده» در سندِ مشخصات. جدولِ سرتاسری با ستونِ راهنمایِ ثابت
  (در موبایل افقی اسکرول می‌شود): تصویر/عنوان/قیمت/تخفیف/موجودی، امتیازِ
  خریداران (فقطِ نظراتِ approved)، متحِدِ کلیدهایِ مشخصاتِ هر چهار کالا
  (جایِ خالی «—»، ترتیبِ نخست‌دید)، و ردیفِ «سازگاری با گوشی» — برتریِ ما.
  ارزان‌ترین ستون با رنگِ برند نشان داده می‌شود. دکمه‌یِ «مقایسه با
  کالاهایِ هم‌خانواده» در صفحه‌یِ کالا، پیوندِ کامل را می‌سازد
  (`/compare?slugs=…`) — قابلِ فرستادن، با کلیدِ بازگشت درست، بدونِ هیچ
  وضعیتِ مرورگری. ۹ آزمونِ تازه (ترتیبِ ستون‌ها، سقفِ چهارتایی، تکرار،
  ناموجود، draft، متحِدِ کلیدها، امتیازِ فقط-approved، سازگاری، بازهٔ
  تخفیف). این تغییرِ CSS دروازهٔ قالب را هم آزمون کرد: `template:check`
  قالبِ کهنه را رد کرد و `npm run template` خروجی را تازه کرد.

- **قالبِ سایت به‌صورتِ یک فایلِ HTML که خودکار تازه می‌ماند** (`scripts/render-qalib.mjs`
  + `docs/qalib.html`، `npm run template` / `npm run template:check`): مرجعِ
  بصریِ کامل — سربرگ/هیرو/نوارِ اعتماد/کارت‌هایِ کالا با دادهٔ نمونه، همهٔ
  توکن‌هایِ طراحی (رنگ/فاصله/شعاع/سایه/تایپ) و کتابخانهٔ اجزایِ ویترین و پنل —
  که **از رویِ همان دو پروندهٔ CSS زنده** (storefront.css + globals.css) و با
  فونت‌هایِ Vazirmatn و پنج تصویرِ کالا **داخلِ خودِ پرونده** (data-URI) ساخته
  می‌شود: یک فایلِ ۸۹۰ کیلوبایتیِ خودکفا، بی‌CDN و بی‌اینترنت. تازه‌ماندنِ
  خودکار دو لایه دارد: ۱) `npm run template` پس ازِ هر تغییرِ طراحی، ۲) دروازهٔ
  CI (`template:check`) با مقایسهٔ sha256ِ هر ورودی با هَشِ ثبت‌شده در سربرگِ
  قالب؛ اگر طراحی عوض شود و قالب بازتولید نشود، زنجیرهٔ کیفیت قرمز می‌شود و
  merge نمی‌دهد (سنجیده شد: تغییرِ رنگِ برند بدونِ بازتولید → خروجِ ۱ با نامِ
  پروندهٔ متخلف؛ بازگرداندن → سبز).

- **کشِ فهرست‌هایِ کاتالوگ** (دسته‌ها/برندها/دستگاه‌ها/رنگ‌ها؛ `query-cache.ts` +
  `cachedList`، `scripts/measure-lists.mjs`): هر بازدیدِ ویترین این چهار فهرست را
  می‌خواند و هیچ‌کدام کش نبود — درختِ دسته‌ها با شمارشِ کالایِ هر دسته رویِ
  ۲۰٬۰۰۵ کالا **۳۴۱ میلی‌ثانیه** بود. حالا با **همان سیاست** `search_cache_seconds`
  (آمارِ جدا در `listCache`ِ observability) کش می‌شوند: هر بازدید ۴۱۶→**۱۰٫۵ ms**
  (۴۰×؛ درخت ۱۶۴×)، نرخِ برخورد ۹۸٫۷٪، بارِ موازیِ ۴۰۰ درخواستِ هم‌زمان با ۰ خطا.
  بی‌اعتبارسازی: نوشتنِ هر کالا/دسته هر دو کش را می‌شکند (فروشنده دستهٔ تازه را
  **همان لحظه** می‌بیند — اثباتِ زنده از API پنل)، `ensureBrand/ensureModel` فقط
  کشِ فهرست را (`clearListCache`) و دکمهٔ پنل هر دو. ۱۲ آزمونِ تازه (۷ واحدی +
  ۵ سرویسِ PGlite).

- **کارگرِ پیامک، فرمانِ استاندارد و بنرِ وضعیت گرفت** (`npm run worker:once`،
  `npm run start:worker` با `POSTSALE_INTERVAL_SEC`، `packages/commerce/src/sms-worker-state.ts`،
  `scripts/fake-sms-provider.mjs`): «پیامک نمی‌رسد» سه علتِ بی‌صدا داشت — سامانه‌ای
  انتخاب نشده، «ارسالِ پیامک» خاموش، یا کارگری که هیچ‌کس بالا نیاورده (رویِ ماشینِ
  توسعه که systemd ندارد اصلاً راهِ استانداردی نبود). حالا پیش از نخستین دور چاپ
  می‌شود **چه چیزی ارسال می‌شود و چرا چیزی ارسال نمی‌شود**، با درمانِ همان خانه
  (پنل ← تنظیمات ← پیامک). حالتِ چرخه با `Ctrl-C` تمیز می‌ایستد (دورِ جاری تمام
  می‌شود، استخرِ اتصال بسته می‌شود) و فاصلهٔ بی‌معنی (`0`، `86400`) را به
  بازهٔ ۵ ثانیه تا ۱ ساعت می‌بندد. `scripts/fake-sms-provider.mjs` هم هر سه
  سامانه را شبیه‌سازی می‌کند تا کلِ مسیر بی‌خریدِ اعتبار سنجیده شود (`sent` با
  `provider_ref`، و ۵۰۰ → تلاشِ دوباره با فاصله)؛ سنجشِ هم‌زمانی: ۲۴ پیام با ۴
  کارگر = ۲۴ ارسال، ۲۴ ارجاعِ یکتا، صفر تکرار.
- **`scripts/` هم زیرِ بررسیِ نوع است** (`tsconfig.scripts.json` +
  `npm run typecheck:scripts`، افزوده‌شده به `verify`): کارگرِ پس از فروش،
  `load-test.mjs`، دریل‌ها و سییدِ مقیاس تا امروز هیچ گِیتی نداشتند — پنج خطایِ
  موجود (`affectedRows` اختیاری، `split('/')[0]`ِ ممکن‌است‌ناشده) برطرف شد.

### اصلاح‌شده

- **آزمون‌هایِ مصرف‌کنندهٔ امضاهایِ تازه** (`categories.test.ts`, `catalog.test.ts`):
  `tree()`/`availableColours()` حالا `{items, cached, cacheAgeMs}` برمی‌گردانند؛
  آزمون‌هایِ قدیمی به شکلِ تازه به‌روز شدند و `product()`ِ دست‌سازِ آن‌ها (نوشتنِ
  دور‌زنِ سرویس) حالا همان کاری می‌کند که `afterCatalogChange` در مسیرِ واقعی
  می‌کند — وگرنه کشِ درختِ آزمونِ پیشین، شمارشِ آزمونِ بعد را خراب می‌کرد.


- **صفِ پیامک با کلیدِ درست هم خالی نمی‌شد** (`packages/commerce/src/sms-send.ts`):
  `varsForTemplate` هر پیامِ بی‌«شناسهٔ قالبِ ارسال‌کننده» را رد می‌کرد، به این
  استدلال که «سامانه‌هایِ ایرانی متنِ آزاد را نمی‌پذیرند» — نادرست: کاوه‌نگار
  (`send`)، ملی‌پیامک (`send/simple`) و فراز هر سه متنِ آزاد را با یک **خطِ پیامک**
  می‌فرستند. یعنی فروشنده‌ای که کلید و خطش درست بود، هیچ‌وقت پیامک نمی‌فرستاد، در
  حالی که دکمهٔ «آزمونِ ارسال» در پنل موفق می‌گفت (دو پاسخِ متعارض به یک سوال).
  حالا `canSendFreeText(cfg)` میانِ «خطِ پیامک» و «قالب» تصمیم می‌گیرد؛ «خطِ
  سرویس» تنها کافی نیست (کاوه‌نگار خودش رد می‌کند، پس تلاشِ سوخته می‌شود) و پیامِ
  رد، **هر دو** درمان را نام می‌برد. چهار آزمونِ تازه رویِ همین نکته اجرا شد.

- **سناریویِ «خریدِ کامل» در آزمونِ بار — پرداخت و رزروِ موجودی زیرِ بار**
  (`scripts/load-test.mjs --mix purchase`، `scripts/purge-load-test-orders.sql`):
  هر چرخه شش فراخوانی است — سبد ← افزودن ← تسویه ← `POST /payments/start` ←
  تصمیمِ درگاهِ آزمایشی ← `POST /payments/verify` — و **همه رویِ یک تنوع**، چون
  مسابقه رویِ یک ردیفِ `stock_items` همان چیزی است که باید دیده شود. یک‌دهم
  خریدها در درگاه «لغو» می‌شوند (مسیرِ برگشتِ موجودی)، `409` «موجودی نیست»
  **پاسخِ درست** شمرده می‌شود نه خطا، و پس از آخرین پله حسابِ انبار بسته می‌شود:
  `on_hand` منفی نشده، کسری **دقیقاً** برابرِ فروشِ تأییدشده است، و رزرو از
  سفارش‌هایِ بی‌پرداخت کمتر نشده — اگر نه، خروجِ ابزار `2` است (در `CI` هم قرمز).
  دو کلید: `--relax-limiter` (ظرفیتِ خام؛ همان کلیدِ اضطراریِ پنل، با بازگرداندن
  در `finally`) و `--with-pages` (برگه‌هایِ سایت؛ با `next dev` نه — ترجمهٔ برگه
  همه‌چیز را می‌خورد). **رویِ همین دستگاه (۲ هسته): ۲۵ خریدارِ هم‌زمان، ۲۰ ثانیه =
  ۴۷٫۲ چرخهٔ خرید در ثانیه (≈۱۸۹ فراخوانی HTTP)، میانهٔ ۹ و ۹۵٪ِ ۱۳ میلی‌ثانیه،
  خطای صفر، ۱۹ فروشِ تأییدشده و ۶۸۸ ردِّ درست؛ پایگاه همان اعداد را تأیید کرد**
  (۲۰ پرداخت موفق، ۲ ناموفق، موجودیِ ۲۲ → ۲، بدونِ فروشِ بیش از انبار). جزئیات و
  چهار باگِ خودِ ابزار که در همین نوبت درآمد، در `docs/pishraft.md` §۲ف۷.

- **کشِ پرسش‌هایِ پُرتکرارِ جستجو + پنلِ «جستجو و مترادف‌ها»**
  (`packages/catalog/src/query-cache.ts`، `packages/catalog/src/synonyms.ts`،
  `apps/api/src/admin-synonyms.controller.ts`، `/admin/search`، مهاجرتِ ۰۳۹):
  پاسخِ یک پرسش تا `search_cache_seconds` (پیش‌فرض ۱۵، خانه‌ای در
  تنظیمات ← پیشرفته، `0` = خاموش **و** خالی‌کردنِ کشِ مانده) در حافظهٔ همان
  فرآیند می‌ماند؛ کلیدِ کش همهٔ گزینه‌ها را می‌پوشاند، پس هیچ‌وقت «کشِ بی‌فیلتر»
  به نتایجِ فیلتردار نمی‌چسبد. هر نوشتنِ کاتالوگ، هر ویرایشِ سازگاری و هر
  مترادفِ تازه کش را همان لحظه می‌اندازد. آمار (`hits/misses/entries/evictions/
  skippedBig/cleared`) در `GET /admin/observability/metrics → searchCache` و
  دکمهٔ «خالی‌کردنِ کشِ جستجو» در پنل هست. فرهنگِ `search_synonyms` (که از
  مهاجرتِ ۰۰۹ بی‌صاحب مانده بود) حالا CRUD کامل در پنل + جدولِ «مردم این‌ها را
  جستجو کردند و چیزی پیدا نشد» (`GET /admin/catalog/synonyms/suggestions`) با
  دکمهٔ «مترادف بساز» دارد. **رویِ ۲۰٬۰۰۵ کالا و PostgreSQL واقعی:** میانگینِ
  زمانِ سرویس ۸۴٫۰۷ → ۲٫۷۱ میلی‌ثانیه (۹۶٫۷٪ برخورد)، پرسش‌هایِ تکراری‌نشده
  ۱۸٫۲۲ → ۱۳٫۰۸، و زیرِ بارِ ۴۰ خریدارِ هم‌زمان توان ۵٫۰۴ → ۱۵٫۵۸ rps با
  میانهٔ تأخیر ۶٬۰۳۱ → ۱٬۳۲۷ میلی‌ثانیه (`scripts/seed-scale.mts` +
  `scripts/measure-search.mjs`؛ جزئیات و جدول‌ها در `docs/pishraft.md` §۲ف۶).
- **`resolvePoolSize`: اندازهٔ استخرِ اتصال با ماشین می‌رود** (`packages/db/src/client.ts`).
  `DB_POOL_MAX ?? 20` رویِ جعبهٔ ۲ هستهٔ توسعه یعنی ۲۰ بک‌اندِ ~۱۰۰ مگابایتیِ
  PostgreSQL و یک صفِ فلج‌کننده — همان «رم پر می‌شود و API هنگ می‌کند». حالا
  پیش‌فرض «دو برابرِ هسته‌هایِ در دسترس، بینِ ۴ و ۲۰» است، `DB_POOL_MAX` برندهٔ
  همیشگی است، و ورودیِ بی‌معنی (`''`، `0`، `-5`) به پیش‌فرض برمی‌گردد — چون
  `max: 0` استخری است که هیچ اتصالی نمی‌دهد و اولین درخواست تا ابد منتظر می‌ماند.
- **مترادفِ چندواژه دیگر بی‌صدا بی‌اثر نیست** (`packages/shared-kernel/src/search.ts`):
  تک‌واژه به گروهِ مترادف‌ها افزوده می‌شود و عبارتِ چندواژه پیش از واکاوی در خودِ
  پرسش جایگزین می‌شود؛ همهٔ جایگزینی‌ها (فرهنگِ داخلی + پنل) در **یک** گذر
  انجام می‌شوند، چون جدول دوطرفه است و جایگزینیِ پشتِ‌سرِ هم عبارت را به
  خودش برمی‌گرداند.
- `scripts/measure-search.mjs` (`--mode hot|unique` با percentiles و `cacheRate`)
  و `scripts/seed-scale.mts` (`--products 20000` / `--clean`؛ ۲۰٬۰۰۰ کالا در ~۱۲ ثانیه).

- **بهداشتِ رسانه در پنل: «چقدر جا آزاد می‌شود» و یک دکمهٔ بی‌خطر**
  (`packages/commerce/src/media-hygiene.ts` + مهاجرتِ ۰۳۸ +
  `GET/POST /admin/media/cleanup`): سه خانه در
  تنظیمات ← پیشرفته — `media_grace_days` (پیش‌فرض ۱۴، ۰ = هرگز و در این حالت
  پوشه پویش هم نمی‌شود)، `media_autopurge` (پیش‌فرض خاموش: فایلِ پاک‌شده
  برنمی‌گردد، پس خودکاری تصمیمِ مدیر است) و `media_scan_minutes` (پیش‌فرض ۳۶۰؛
  پویشِ پوشه یعنی یک `stat` به‌ازایِ هر فایل، پس نتیجه کش می‌شود — و کش عمداً
  **بی‌ردِّ عملیات** نوشته می‌شود، وگرنه هر دورِ کارگر یک سطرِ ممیزی است).
  خودِ پیش‌نمایش هم سقفِ زمان دارد (`MEDIA_REPORT_BUDGET_MS = 400`) تا پرسشِ
  «چند فایلِ بی‌صاحب؟» هیچ‌وقت به قیمتِ کُند شدنِ صفحهٔ کالا درنیاید: شمارشِ
  نیمه «کمترینِ قطعی» گفته می‌شود، «ردیفِ بی‌فایل» را گزارش نمی‌کند و کش هم
  نمی‌شود. «از نو بررسی کن» و خودِ پاک‌سازی بی‌سقف‌اند.
  صفحهٔ تصویرِ کالا حالا می‌گوید «چند فایلِ بی‌صاحب · چه حجمی · مهلتِ اعتماد ·
  خودکار روشن است یا نه»، دکمهٔ «از نو بررسی کن» دارد، و «N ردیف به فایلی اشاره
  می‌کند که رویِ دیسک نیست» را به‌عنوانِ هشدار می‌گوید. کارگرِ پس از فروش هم پنج
  کار دارد (پنجمی: بهداشتِ فایل — با `POSTSALE_MEDIA_LIMIT`)، و صفحهٔ
  `GET /admin/observability/cleanup` هم بلوکِ `media` را در همان پیش‌نمایش می‌آورد
  (پیش‌نمایش؛ حذفِ فایل از آن‌جا انجام نمی‌شود). سنجش: ۱۵ + ۱۳ آزمونِ تازه رویِ
  PGlite با دیسکِ واقعی، `scripts/pg-media-drill.mts` (۲۵ سنجش روی PostgreSQL با
  فایل‌هایِ واقعی) و `scripts/http-media-drill.mts` (۳۳ سنجش از لایهٔ HTTP با
  بارگذاریِ واقعیِ تصویر: فایلِ جداشده رفت و فایلِ بنر ماند).

- **نگهداریِ داده و پاک‌سازیِ خودکار** — تا پیش از این پالایشِ «خودکار» فقط
  رویِ کاغذ بود: `observability_retention_days` هیچ‌جا خوانده نمی‌شد (فقط در
  توضیحِ مهاجرتِ ۰۳۳ نامش بود) و کاری که واقعاً می‌شد یک `DELETE` هاردکدِ
  ۷روزه در تایمرِ API بود؛ `sms_outbox` — که متنِ پیامک و شمارهٔ موبایلِ مشتری
  در آن است — **هیچ** مسیرِ پاک‌سازی نداشت. حالا یک پیاده‌سازی هست و همه از آن
  می‌خوانند: `packages/commerce/src/housekeeping.ts` (`RETENTION_DEFAULTS` /
  `RETENTION_KEYS` / `readDays` / `readRetentionPolicy` / `purgeExpiredData`)،
  مهاجرتِ ۰۳۷ (کاشتِ `sms_outbox_retention_days='60'` و
  `audit_log_retention_days='0'` + توضیحِ درست برای `observability_retention_days`)،
  تیکِ هر ۵ دقیقهٔ API، کارِ چهارمِ `scripts/post-sale-worker.ts`
  (`POSTSALE_PURGE_LIMIT`، پیش‌فرض ۲۰۰۰)، و `GET/POST /admin/observability/cleanup`
  (پیش‌نمایشِ بی‌حذف‌کردن + اندازهٔ جدول‌ها با `pg_total_relation_size`، و اجرا با
  ممیزیِ فقط‌شمارشی). قواعد: `0` یعنی «هرگز پاک نشود»؛ فقط `sent`/`dead`ِ پیامک
  واجدِ شرط‌اند (صفِ زنده هرگز); مهلتِ ممیزی پیش‌فرض صفر است چون تنها سندِ
  فروشنده در اختلافِ مالی است؛ سیاست در هر اجرا از نو خوانده می‌شود (تغییر از پنل
  بی‌restart اثر می‌کند)؛ و پاک‌سازی زیرِ `pg_advisory_xact_lock` انجام می‌شود تا
  تایمرِ API و کارگرِ پس از فروش کارِ تکراری نکنند. در پنل: سه خانهٔ عددیِ تازه و
  pillِ «نگهداریِ پیامک: N روز · M موردِ آمادهٔ پاک‌سازی» در تبِ صندوقِ پیامک.
  ۱۹ آزمونِ PGlite + `scripts/pg-retention-drill.mts` (۲۶ سنجش روی PostgreSQL
  واقعی) + `scripts/http-retention-drill.mts` (۲۴ سنجش از لایهٔ HTTP: RBAC،
  رقمِ فارسی در تنظیمات، dryRun، LIMITِ بی‌سقف ← ۴۰۰). چهار درسی که همین کار
  داد: (۱) **شمارش و حذف باید یک SQL باشند** — دو نسخهٔ دستی‌نوشتهٔ شرطِ
  «تمام‌شده»، در PGlite سبز بود و رویِ سرور پیامک‌های `pending` را می‌کشت؛ (۲)
  `Number('') === 0` یعنی «بی‌تنظیم، همه‌چیزِ کهنه را بریز دور» — `readDays`
  اولِ خالی‌بودن را می‌بیند و `0` را به‌عنوانِ تصمیم می‌پذیرد (نه با `||`)؛
  (۳) `pg_advisory_lock` زیرِ استخرِ اتصال درست کار نمی‌کند (قفل تا مرگِ اتصال
  می‌ماند و unlock ممکن است از اتصالِ دیگری باشد) — `pg_advisory_xact_lock` داخلِ
  `db.transaction`، و بی‌قفل وقتی هندل تراکنش ندارد (فراخوانیِ تودرتو)؛ (۴)
  `fallback` در کاتالوگِ پنل باید **مساویِ** `RETENTION_DEFAULTS` باشد وگرنه پنل
  عددی نشان می‌دهد که کد هرگز آن را اعمال نمی‌کند.
- `setup.sh` در ریشهٔ مخزن: تشخیص و نصبِ پیش‌نیازها (Node، PostgreSQL با
  brew/apt/dnf/pacman/zypper)، ساختِ خوشهٔ پایگاهِ خصوصی بیرون از مخزن،
  مهاجرت‌ها، نشاندنِ کاتالوگ فقط در پایگاهِ خالی، و اجرایِ وب و API — با
  فرمان‌هایِ `check` / `install` / `db` / `seed` / `status` / `logs` / `stop`
  و گزینهٔ `--prod` برایِ ساختِ بهینه. رازها در `~/.setshop` می‌نشینند و به
  مخزن نمی‌آیند. (`README.md` §اجرا، `docs/pishraft.md` §۲ن)
- `SETUP-LOCAL.md`: سه فرمان برایِ بالا آوردنِ پروژه رویِ مک/لینوکس، جدولِ
  پیش‌نیازها، و رفعِ هفت خطایِ رایج (Node کهنه، keg-only بودنِ brew، پورتِ
  اشغال، خوشهٔ نیمه‌بند، نبودِ sudo).
- **«چرا پیامک نمی‌رسد؟» در پنل** (`GET /admin/settings/sms/readiness` +
  `POST /admin/settings/sms/outbox/requeue` + `packages/commerce/src/sms-diagnose.ts`):
  چک‌لیستِ آمادگی (ارائه‌دهنده، کلید، روشن‌بودنِ ارسال، `SMS_DRY_RUN`، قالب‌هایِ
  بی‌شناسه، سنِ صف، قفلِ «در حالِ ارسال»ِ کارگرِ مرده) و دکمهٔ «بازگرداندنِ N پیام
  به صف» — چون پیامکی که به‌خاطرِ «شناسهٔ قالبِ خالی» به `dead` رفته، با پرکردنِ
  آن خانه **خودبه‌خود** برنمی‌گردد و پیش از این رفعِ اشکال یعنی ویرایشِ دستیِ
  `sms_outbox`. سه قاعده: پیامِ `sent` هرگز دست نمی‌خورد (حتی با `force`)؛ بی‌`force`
  فقط خطاهایِ تنظیمی برمی‌گردند؛ و «عددِ قابلِ بازگردانی» از همان `REQUEUE_GATE_SQL`
  خوانده می‌شود که `requeueSmsMessages` اجرا می‌کند، پس دکمه هرگز کمتر از شمارهٔ
  رویِ دکمه کار نمی‌کند. بیست‌و‌سه آزمونِ تازه + سنجشِ پایانه‌ای روی PostgreSQL
  واقعی (RBAC، ممیزی بدونِ شماره، چرخهٔ «پرکردنِ قالب ← بازگردانی ← ارسال»).
  در همین مسیر یک ۵۰۰ِ واقعی گرفته شد: `INSERT INTO audit_logs (… ip_address,
  user_agent)` — دو ستونی که وجود ندارند؛ آزمونِ واحد هرگز کنترلر را اجرا نکرده
  بود. (`docs/pishraft.md` §۲ف٢)
- **«کارگرِ صف خوابیده؟» — پایشِ صفِ پیامک در پنل** (`GET
  /admin/settings/sms/stats?hours=…` + `packages/commerce/src/sms-monitoring.ts`
  + `packages/db/migrations/036_sms_last_attempt.sql`): نمودارِ ستونیِ ساعتی از
  صف (در نوبت / فرستاده‌شده / ناموفق / ناامیدکننده)، «آخرینِ ارسال»، «آخرینِ
  تکانهٔ صف» و بنرِ قرمزِ **خوابِ کارگر** — چون «چرا پیامک نمی‌رسد؟» خانه‌هایِ
  خالیِ تنظیمات را می‌گوید، اما این حالت را نه: تنظیمات کامل است، صف پُر است و
  هیچ‌کس صف را نمی‌فرستد (تایمرِ systemd غیرفعال، کارگر رویِ سرورِ اشتباه). آستانه
  از پنل تنظیم می‌شود (`sms_worker_stale_minutes`، پیش‌فرض ۷۰ دقیقه) تا با فاصلهٔ
  اجرایِ تایمر هم‌راستا بماند. دو ستون/تصمیمِ ساختاری: `sms_outbox.last_attempt_at`
  که در لحظهٔ برداشتنِ پیام نوشته می‌شود (پیش از این «تکانه» از `available_at`
  حدس زده می‌شد و آن *نوبتِ بعد* است، پس قطعیِ سامانه «کارگر تازه بود» گزارش
  می‌شد) و شبکهٔ سطل‌ها که خودِ SQL می‌سازد (`WITH RECURSIVE`)، چون ساختنِ آن در
  JS باعث می‌شد پیامکی که SQL شمرده در هیچ سطلی جا نشود. چهار باگِ این مسیر —
  نادیده‌گرفتنِ گامِ `generate_series` در PGlite، رشته‌بودنِ `timestamptz` در
  `pg` (نه `Date`)، رقمِ لاتین و «null دقیقه» در جمله‌هایِ فارسی — همگی با
  سنجشِ رویِ PostgreSQLِ واقعی گرفته شدند، نه با typecheck و نه با PGlite.
  چهارده آزمونِ تازه + دو اسکریپتِ سنجشِ پایانه
  (`scripts/pg-stats-drill.mts`، `scripts/http-stats-drill.mts`).
  (`docs/pishraft.md` §۲ف٣)
- **«آزمونِ ارسال» در پنل** (`POST /admin/settings/sms/test`، تبِ تازه‌یِ
  «آزمونِ ارسال» در تنظیمات ← پیامک): یک پیامکِ تک به شماره‌یِ خودِ مدیر،
  بی‌نوشته‌شدن در `sms_outbox`، با پاسخِ خامِ سامانه (`raw`)، زمانِ رفت‌و‌برگشت،
  شناسهٔ قالب، متغیرهایِ ارسالی و **متنی که واقعاً می‌رود** — تا «اعتبار
  تمام‌شده» از «قالب تأییدنشده» و «کلید غلط» در دو دقیقه جدا شود. با
  `SMS_SAMPLE_VARS` مقدارهایِ نمونه پر می‌شوند (متنِ همان قالب، نه `{order}`
  خام). سقفِ پنج آزمون در ده دقیقه برایِ هر حساب (اعتبارِ واقعی مصرف
  می‌شود)، ممیزی با شمارهٔ پوشانده، و همان پالایشِ «فقط ایران» + مهلتِ
  درخواست که کارگر دارد. سیزده آزمونِ تازه در `sms-send.test.ts` (۳۳ ← ۴۶).
- **دو دریچهٔ بازِ وعده‌داده‌شده که در کد نبودند**: `SMS_EXTRA_HOSTS` حالا
  واقعاً فهرستِ مجازِ میزبان‌ها را باز می‌کند (`extraHostsFromEnv` +
  `guardRequest({ extraHosts })`) و `SMS_TIMEOUT_MS` حالا واقعاً اعمال می‌شود
  (`guardedFetchWithTimeout`) — مهلت با `Promise.race` و تایمرِ خودی است، نه
  تنها با `AbortSignal`، چون یک `fetch` بی‌پاسخِ جاوااسکریپتی سیگنال را نادیده
  می‌گیرد و کارگرِ `oneshot` تا ابد می‌خوابد؛ سیگنال هم فرستاده می‌شود تا سوکت
  نشت نکند، و `abort` تنها در شاخهٔ مهلت صدا می‌خورد (وگرنه بدنهٔ پاسخِ سالم
  هنگامِ خواندن می‌شکند). `SmsProviderError` حالا `raw` را هم نگه می‌دارد.
- **ارسالِ واقعیِ پیامک** (`packages/db/migrations/035_sms_dispatch.sql`,
  `packages/commerce/src/sms-send.ts`): صفِ `sms_outbox` از «فقط نوشتن» به
  «فرستادن» رسید — `attempts`، `last_error` و `available_at` (تلاشِ دوباره با
  فاصلهٔ دوبرابر تا سقفِ یک روز، و «ناامیدکننده» پس از `sms_max_attempts`)؛
  قفلِ `SKIP LOCKED` تا دو کارگر هم‌زمان یک پیامک را نفرستند و آزادکردنِ
  پیام‌هایِ گیرمانده در `sending`. هر درخواستِ خروجی از پالایشِ
  `guardRequest` می‌گذرد (فقط `https`، و میزبان‌هایِ `api.kavenegar.com` /
  `api.melipayamak.com` / `sms.farazsms.com` در فهرستِ مجاز؛ بقیه با
  `OutsideIranError` رد می‌شوند — `SMS_EXTRA_HOSTS` و `SMS_ALLOW_INSECURE` تنها
  راه‌هایِ بازکردنِ آگاهانه‌اند). سه ارائه‌دهنده پیاده شد و «ارائه‌دهندهٔ
  ناآشنا» بی‌صدا به `none` برمی‌گردد و دلیلش را در نتایج می‌نویسد. چون در
  صف فقط متنِ رندرشده می‌ماند، مقدارِ جای‌نگهدارها از همان متن و قالبِ اصلی
  بیرون کشیده می‌شود (`splitTemplate` / `extractVars`) — و نه نامِ متغیرها؛
  این دقیقاً همان باگی بود که در سنجشِ پایانه‌ای با فرستندهٔ ساختگی گرفته شد.
  کارگرِ پس از فروش (`scripts/post-sale-worker.ts`) حالا سه کار دارد و پیامک‌ها
  را هم می‌فرستد (`SMS_BATCH`، پیش‌فرض ۲۰)؛ سه کلیدِ تازه در پنل
  (`sms_sender`، `sms_max_attempts`، `sms_retry_minutes`)؛ `POST
  /admin/settings/sms/outbox/flush` با دسترسیِ جداگانهٔ `sms.outbox.send`
  («همین حالا بفرست» در تنظیمات ← صندوقِ پیامک، با ستونِ «تلاش» و نمایشِ
  آخرینِ خطا زیرِ هر پیام). سی‌و‌سه آزمونِ واحد و یک سنجشِ پایانه‌ای روی
  فرستندهٔ ساختگی. (`docs/pishraft.md` §۲ف، `SETUP-LOCAL.md` §پیامکِ واقعی)
- **مسیرِ اعتمادِ نشانیِ مشتری** (`apps/api/src/client-ip.ts` +
  `client-ip-hook.ts`): `parseTrustedProxies()` (IP یا CIDR، هر دو نسخه) و
  `resolveClientIp()` که سرآیندهایِ `X-Real-IP` / `X-Forwarded-For` /
  `x-set-client-ip` را **فقط** از همتایِ داخلِ `TRUSTED_PROXY_IPS` می‌پذیرد؛
  `X-Real-IP` مقدم است و در نبودش آخرین عضوِ XFF (چون nginx نشانیِ واقعی را
  در آخر می‌گذارد). قلابِ `onRequest` همان مقدار را رویِ `request.ip` می‌نشیند
  تا مهارِ نرخ، تلاش‌هایِ ورود، ممیزی، بارِ پرداخت و لاگِ درخواست یک نشانیِ
  صادقانه ببینند. هفده آزمونِ تازه (۱۰ حلّال + ۵ قلاب + ۲ بدهبستانِ موجودی در
  `packages/orders`). (`docs/pishraft.md` §۲ع)
- `TRUSTED_PROXY_IPS` و `TRUST_WEB_CLIENT_IP` در تنظیماتِ API، و
  `TRUSTED_PROXY_IPS=127.0.0.1,::1` / `TRUST_WEB_CLIENT_IP=true` /
  `TRUST_XFF=` در `deploy/env.example` با توضیحِ «چه زمانی لازم است».
- آزمونِ بار: `--mix checkout` (سبد ← افزودن ← تسویه ← رزروِ موجودی ← ثبتِ
  سفارش) و `--real-ip=<IP پروکسی>` تا خودِ ابزار هم nginx را بازی کند و هر
  خریدار نشانیِ یکتا بگیرد؛ نگهبان‌هایِ `no-cart`/`fill-failed` (پیش‌تر
  `/cart/null/items` صدا می‌زد و خطایِ ابزار را خطایِ سرور گزارش می‌کرد)، و
  `--only` برایِ سنجشِ یک گام.
- دو اندازه‌گیریِ ثبت‌شده در `docs/bar-va-tavan.md` §۷: با نشانیِ مشترک
  ۷۸۳ درخواست / ۶۳٫۱ rps / **۱۸٫۱۴٪ خطا (۴۲۹×۱۳۸)** و ۹۵٪ِ ۲۵ میلی‌ثانیه؛
  با نشانیِ مجزا **۷ ۲۴۷ درخواست / ۳۴۲٫۶ rps / خطای صفر** و ۹۵٪ِ ۹۲
  میلی‌ثانیه در بارِ ۳۰۰ خریدارِ هم‌زمان (همین دستگاه، موجودیِ مصنوعیِ پُر —
  توضیحش در همان بند).

- پیام‌هایِ روشن برایِ نبودِ Node یا git (پیش‌تر خطا در میانهٔ نصب می‌آمد)، و
  خواندنِ درستِ نسخهٔ PostgreSQL در `check` (`${arr[0]}` درونِ ‎$( … )‎ِ
  نقل‌قول‌شده به‌صورتِ متن پارس می‌شد).

- **درگاهِ آزمایشی رویِ محیطِ توسعه ۴۰۳ می‌داد** (`apps/api/src/payments.controller.ts`):
  نگهبانِ `POST /payments/sandbox/:authority/decision` تنها
  `PAYMENT_ALLOW_SANDBOX=1` را می‌پذیرفت، در حالی که خودِ سرویس و
  `docs/pardakht-iran.md` قراردادشان این بود «توسعه باز، تولید با کلید»
  (`allowSandbox = PAYMENT_ALLOW_SANDBOX === '1' || NODE_ENV !== 'production'`).
  نتیجه: رویِ هر کلونِ تازه، نخستین آزمونی که آدم برایِ دیدنِ «پرداخت موفق/لغو»
  می‌زند ۴۰۳ می‌گرفت و `--mix purchase` آن را «پرداختِ ناموفق» می‌شمرد. نگهبان
  همان قراردادی را می‌خواند که سرویس اعلام می‌کند و پیامِ ۴۰۳ نامِ کلیدِ تولید را
  می‌نویسد. `sandboxAllowed()` بیرون کشیده شد و `apps/api/src/payments.controller.test.ts`
  تطابقِ قاعده‌یِ نگهبانِ HTTP با `PaymentService.config()` را در سه محیط و دو
  حالتِ کلید می‌سنجد — چون «دو نگهبانِ یک در» دقیقاً همان‌جا است که بازمی‌گردد.
- **پاک‌سازیِ فایل‌هایِ رسانه، فهرستِ مراجع را کامل نمی‌دید** — خطرناک‌ترین باگی
  که تا امروز در این مخزن پیدا شد. `sweepOrphanFiles` (دکمهٔ «پاک‌سازیِ فایل‌هایِ
  بی‌صاحب» در صفحهٔ تصویرِ کالا) تنها `product_images` را می‌خواند، ولی نشانیِ
  `/media/…` در سه جایِ دیگر هم ذخیره می‌شود: `store_banners.image_url` (بنرِ
  صفحهٔ نخست), `product_types.image_url` (تصویرِ دسته) و `checks.image_url`
  (اسکِنِ سندِ چک). یعنی یک روبش می‌توانست تصویرِ **در حالِ نمایشِ** بنر یا تنها
  سندِ یک چکِ برگشتی را برایِ همیشه از دیسک بردارد — بی‌خطا، بی‌ممیزی، بی‌بازگشت.
  حالا `MEDIA_SOURCES` هر شش ستون را می‌شمارد، `packages/media/src/hygiene.test.ts`
  از `information_schema` می‌سنجد هیچ ستونِ ارجاعی جا نیفتاده باشد، و دو چک‌بر
  («پایگاه هیچ ارجاعی ندارد ولی دیسک پُر است» / «بیش از نیمی از دیسک بی‌صاحب
  شمرده شد») جلویِ حذفِ انبوهِ غیرعادی را می‌گیرند؛ بی‌`force` در این دو حالت صفر
  فایل می‌رود و یک جملهٔ توضیح می‌ماند؛ اگر مدیر خواست با وجودِ هشدار اجرا کند،
  همان صفحه دکمهٔ «با وجودِ این هشدار پاک کن» را نشان می‌دهد (نه چک‌باکسی پنهان).
  افزوده‌هایِ همان مسیر: فایل‌هایی که نامشان
  شکلِ فایلِ سامانه نیست **شمرده می‌شوند ولی پاک نمی‌شوند**، پنجرهٔ اعتمادِ
  `media_grace_days` (پیش‌فرض ۱۴ روز، کفِ سخت ۶۰ دقیقه، «۰ = هرگز — و پویش هم نه»)
  گزارشِ «ردیفِ بی‌فایل» (تصویرِ شکستهٔ فروشگاه، که پیش‌تر هیچ‌جا دیده نمی‌شد)،
  و شمارشِ `*.tmp`هایِ نیمه‌کارهٔ کهنه (با محافظتِ ۱۵ دقیقه‌ایِ نوشتنِ در جریان).
  `POST /admin/media/sweep` به‌عنوانِ مسیرِ سازگاری ماند و همان گزارش را می‌دهد.

### تغییرکرده

- **`scripts/load-test.mjs` دو چیز یاد گرفت — هر دو از یک یخ‌زدگیِ واقعی:**
  **فیوزِ اشباع** (`--abort-after`، پیش‌فرض ۶۰ خطایِ پشتِ سرِ هم): ادامهٔ کوبیدن
  رویِ سرویسی که جواب نمی‌دهد ماشینِ آزمون را هم قفل می‌کند و عددِ جا‌مانده سقفِ
  فروش نیست؛ و **جداشمردنِ ۴۲۹** از خطاها — مهارِ نرخِ خودِ سرویس «فروش رفت»
  نیست، «این بار بیش از توانِ جعبه بود» است. گزارشِ `cached` هم از بدنهٔ پاسخ
  خوانده می‌شود: `از کش: جستجو (API) ۵۰ از ۱۰۷ (۴۷٪)`.
- **`trustProxy: true` در API به `false` عوض شد** (رفعِ حفرهٔ جعلِ نشانی):
  پیش‌تر هر بازدیدکننده با `X-Forwarded-For: 10.0.0.7` می‌توانست از سقفِ
  «۱۰ سفارش در ساعت» و «۱۰ تلاشِ ناموفقِ ورود در ۱۰ دقیقه» بیرون بزند —
  سنجیده شد: شش درخواستِ تسویه از ۱۲۷.۰.۰.۱ → `429×6`، همان شش با سرآیندِ
  جعلی → `201×6`. جایِ آن، فهرستِ صریحِ `TRUSTED_PROXY_IPS` است.
- **لایهٔ وب نشانیِ مشتری را به API می‌رساند** (`x-set-client-ip` +
  `x-set-internal`، تنها در مسیرهایِ نوشتن و در میانجی‌هایِ جستجو و
  تصمیمِ پرداخت؛ `get()` کشیدنی دست‌نخورده تا ISRِ برگه‌ها نشکند). بی‌این،
  پس ازِ بستنِ حفرهٔ بالا، همهٔ مهمان‌ها رویِ نشانیِ خودِ سرور می‌نشستند و
  سقفِ هر خریدار به سقفِ کلِ فروشگاه تبدیل می‌شد.
- **صفحهٔ جستجو دیگر سه خواندنیِ ترتیبی انجام نمی‌دهد.** رنگ‌ها مسیرِ خودشان را
  دارند (`GET /catalog/colours` در برابرِ خواندنِ صد کالا)، خواندنی‌هایِ مستقل
  موازی شدند، و `revalidate = 15` جایِ `force-dynamic` را گرفت. در بارِ ۳۰۰
  خریدارِ هم‌زمان: جستجو ۳۴۵ ← **۸۰ میلی‌ثانیه**، ۹۵٪ِ پاسخ ۴۲۶ ← **۱۱۶
  میلی‌ثانیه**، توان ۲۹۵ ← **۳۴۱** درخواست در ثانیه (همان دستگاه، دو فرآیندِ
  وب). نکتهٔ راست: خودِ پاسخِ HTTP کش نمی‌شود (برگه از `searchParams`
  می‌خواند) — آنچه کش می‌شود داده است؛ جزئیات و عدد در `docs/bar-va-tavan.md`
  §۶.

## [0.1.0] — ۱۴۰۴/۰۶/۲۶ (۱۷ سپتامبر ۲۰۲۶)

نخستین انتشارِ مخزن: کلِ سامانه‌ای که تا امروز ساخته شده، با دروازه‌هایِ
کیفیتِ خودکار روی گیت‌هاب.

### افزوده‌شده

**هسته و داده**
- پایگاه با ۲۶ مهاجرتِ شماره‌گذاری‌شده — از طرحِ نخستین تا رسانه، مالیات،
  مرجوعی و تأمین.
- لایه‌یِ دسترسی با adapters برای پایگاهِ واقعی و حافظه (`memory://`).

**ماژول‌هایِ کسب‌وکار** (هر کدام با آزمونِ اختصاصی)
- کاتالوگ: کالا، تنوع، سازگاری با مدلِ گوشی، جستجویِ فارسی (نرمال‌سازی و
  هم‌ارزیِ «ی/ک»).
- موجودی: رزروِ اتمیک، تأیید، آزادسازی، انقضایِ خودکار.
- سفارش و سبدِ خرید: رزرو در تراکنش، کلیدِ یکتایی، سندِ فروش.
- صندوقِ حضوری: شیفت، فروشِ آفلاین، مغایرتِ نقدی.
- حسابداری: دفترکلِ دوبل، فاکتورِ خرید، ارزشِ افزوده، بهایِ میانگینِ موزون.
- تأمین و تسویه با تأمین‌کننده.
- مالیات: صفِ صورتحساب، شناسه‌یِ یکتایِ مالیاتی (الگویِ مؤدیان)، اعتبار.
- مرجوعی و گارانتی: از درخواست تا بازگشتِ وجه، با سند و صورتحسابِ اصلاحی.
- گزارش‌ها و نقش‌ها/دسترسی‌ها.

**رابط‌ها**
- وب‌سایتِ فروشگاهی: Next 15، راست‌چین، فونت و تصویرِ محلی، جستجو، سبد،
  تسویه، حسابِ کاربری.
- پنلِ مدیریت: نشست، پیشخوان، کالاها، سفارش‌ها، موجودی، صندوق، حسابداری،
  مالیات، مرجوعی.
- **بارگذاریِ تصویر از پنل**: کشیدن و رها کردن، ساختِ خودکارِ سه اندازه و
  پیش‌نمایشِ تار، گزینشِ تصویرِ اصلی، روبشِ فایل‌هایِ بی‌صاحب.

**کیفیت و استقرار**
- بازرسِ پروژه با ۶ قانونِ الزامی (`scripts/check-code.mjs`).
- اجرایِ خودکار روی گیت‌هاب (`.github/workflows/ci.yml`): بررسیِ نوع،
  بازرسی، ساخت، و آزمون‌ها با سرویسِ Postgres.
- اسکریپت‌هایِ استقرار (`deploy/`): نصب، واحدهای systemd، پشتیبان و
  بازگردانی — پوشه‌یِ رسانه همراهِ پایگاه پشتیبان گرفته می‌شود.
- مستندات: `README.md`، `AGENTS.md` (قراردادِ کاری)، و پنج سندِ موضوعی در
  `docs/` به‌همراهِ اسنادِ مشخصاتِ کارفرما در `docs/moshakhasat/`.

### وضعیت

- **۳۲۷ آزمون در ۲۵ پرونده — همه سبز.**
- دروازه‌ها: بررسیِ نوع (ریشه و وب) ✅ · بازرسی ✅ · ساختِ وب ✅.

### دانسته‌نشده / مانده

- محدودسازیِ نرخ و کاوش (Prometheus/OpenTelemetry).
- خروجیِ اکسل و PDFِ رسمی (فعلاً CSV با BOM).
- اتصالِ واقعی به سامانه‌یِ مؤدیان — نیازمندِ گواهی و کلید از سازمان.
- آزمونِ بارِ هزاران کاربرِ هم‌زمان.
- وابستگی‌ها از نظرِ امنیت بررسی شده اما ارتقا نیافته‌اند (نیازمندِ تأیید).
## Unreleased

- محدودسازی API پرداخت: آغاز پرداخت و خواندن وضعیت/authority فقط از تماس داخلی وب؛
  callback و verify عمومی لازم برای درگاه حفظ شد.
- افزودن مسیرهای مدیریت تعرفه و وضعیت مرسوله، مدیریت چک مشتری، permissionهای
  `shipping.*` و `checks.*`، و اصلاح ثبت وضعیت قبلی در audit چک.
