import { ObservabilityPanel } from '@/components/admin/observability-panel';

export const dynamic = 'force-dynamic';

export const metadata = { title: 'سلامت و مهارِ بار — پنلِ ست‌شاپ' };

/**
 * سلامتِ سامانه و مهارِ بار.
 *
 * چرا این صفحه در پنل است؟ چون دو پرسشِ همیشگیِ مدیر این‌هاست:
 *
 *   ۱. «سایت کُند است — کجایش کُند است؟» بی‌داده، پاسخ حدس است و هر حدسی
 *      یک مقصرِ بی‌گناه می‌تراشد (پایگاه، شبکه، «اینترنتِ کشور»). اینجا
 *      کندترین مسیرها با **صدکِ ۹۵** می‌آیند، نه با میانگین: میانگین یک
 *      درخواستِ دوثانیه‌ای را در میانِ صد درخواستِ بیست‌میلی‌ثانیه‌ای قایم
 *      می‌کند، و همان یک درخواست است که مشتری را فراری می‌دهد.
 *
 *   ۲. «این سقف‌ها دستِ کیست؟» مهارِ بار اگر از پنل تنظیم نشود، یا برای
 *      همیشه در کد می‌ماند (و کسی جرئتِ تغییرش را ندارد) یا باید با دست
 *      در پایگاه دستکاری شود. هر دو یعنی مدیر بر چیزی که مستقیم رویِ فروش
 *      اثر دارد، کنترلی ندارد.
 *
 * یک نکته‌یِ صادقانه که در خودِ صفحه هم نوشته شده: آمار از حافظه‌یِ همین
 * نمونه‌یِ API است؛ اگر پشتِ چند نمونه اجرا شود، هر کدام آمارِ خودش را
 * دارند. سقف‌ها و ردِّ مسدودشدن‌ها در پایگاه‌اند و یکسان‌اند.
 */
export default function ObservabilityPage() {
  return (
    <div className="page">
      <header className="page__head">
        <h1 className="page__title">سلامت و مهارِ بار</h1>
        <p className="page__sub">
          ببینید سامانه چقدر بار می‌کشد، کدام مسیر کند است، و سقفِ هر بخش را
          خودتان تنظیم کنید — بی‌تغییرِ کد و بی‌دسترسی به سرور.
        </p>
      </header>

      <ObservabilityPanel />
    </div>
  );
}
