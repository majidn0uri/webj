import { CouponsPanel } from '@/components/admin/coupons-panel';

export const dynamic = 'force-dynamic';

export const metadata = { title: 'کوپن و کدِ تخفیف — پنلِ ست‌شاپ' };

/**
 * مدیریتِ کوپن و کدِ تخفیف.
 *
 * تا پیش از این، کلیدِ «کوپن» در تنظیمات بود بی‌آنکه چیزی پشتش باشد: درست
 * بودنش درست نبود. اینجا فروشنده خودش کمپین می‌سازد — درصدی یا مبلغی، با
 * سقف، با کمینه‌یِ خرید، ویژه‌یِ یک کالا یا یک دسته، با سهمیه‌یِ هر مشتری —
 * و می‌بیند هر کد چند بار مصرف شد.
 *
 * نکته‌یِ پولی: تخفیفِ کوپن پایه‌یِ مالیات را هم می‌کاهد، چون ارزش‌افزوده
 * بر مبلغِ پس از تخفیف بسته می‌شود، نه پیش از آن. پس سفارشِ با کوپن،
 * مالیاتِ کمتری هم دارد — و این درستِ قانونیِ ماجراست.
 */
export default function CouponsPage() {
  return (
    <div className="page">
      <header className="page__head">
        <h1 className="page__title">کوپن و کدِ تخفیف</h1>
        <p className="page__sub">
          کد بسازید، محدودش کنید، و ببینید چند بار مصرف شد. کدِ خاموش یا منقضی
          در تسویه کار نمی‌کند؛ نوبتش هم با لغوِ سفارش به مشتری برمی‌گردد.
        </p>
      </header>

      <CouponsPanel />
    </div>
  );
}
