/**
 * پوسته‌یِ فروشگاه: هدر، فوتر و نوارِ تبِ موبایل.
 *
 * چرا این پوسته در یک «گروه مسیر» جدا نشست و نه در چیدمانِ ریشه؟
 *
 * چون پیش از این، چیدمانِ ریشه برای اینکه بداند «در پنل هستیم یا در
 * فروشگاه»، سرآیندِ درخواست را می‌خواند (`headers()`). یک فراخوانِ کوچک با
 * یک پیامدِ بزرگ: خواندنِ سرآیند یعنی این چیدمان **پویا** است، و چون همه‌یِ
 * صفحه‌ها زیرِ آن‌اند، هیچ صفحه‌ای از فروشگاه دیگر کش نمی‌شد — نه خانه، نه
 * دسته، نه برگه‌یِ کالا. هر بازدید یعنی رندرِ دوباره‌یِ همان برگه برایِ همان
 * هزار نفر.
 *
 * اندازه‌یِ این تصمیم در آزمونِ بار دیده شد: توان در حدودِ ۷۰ برگه در ثانیه
 * رویِ دو هسته قفل می‌شد، در حالی که خودِ API در ۲ میلی‌ثانیه پاسخ می‌داد و
 * پردازنده‌اش خلوت بود — یعنی هزینه از رندر می‌آمد، نه از داده.
 *
 * حالا مرز با **ساختارِ پوشه‌ها** گفته می‌شود، نه با خواندنِ درخواست: صفحه‌هایِ
 * فروشگاه در این گروه‌اند و پوسته را می‌گیرند؛ پنل در شاخه‌یِ خودش است و
 * نمی‌گیرد. نشانی‌ها هیچ تغییری نکرده‌اند (گروه‌هایِ مسیر در نشانی دیده
 * نمی‌شوند) و چیدمانِ ریشه دیگر هیچ چیزی از درخواست نمی‌خواند — پس برگه‌هایِ
 * فروشگاه می‌توانند واقعاً کش شوند.
 */

import { StoreHeader } from '@/components/store/store-header';
import { StoreFooter } from '@/components/store/store-footer';
import { TabBar } from '@/components/store/tab-bar';

export default function StoreLayout({ children }: { children: React.ReactNode }) {
  return (
    <>
      <StoreHeader />
      <main>{children}</main>
      <StoreFooter />
      <TabBar />
    </>
  );
}
