# فلوچارت جامع فروشگاه SET

## بررسی کامل از منظر ۴ نوع خریدار + مدیر

تاریخ: ۲۰۲۶-۰۹-۲۰

---

## ۱. خریدار آنلاین (پرداخت اینترنتی)

```
بازدیدکننده → صفحه اصلی (/) → جستجو یا دسته‌بندی → لیست محصولات (/search, /c/...)
    → جزئیات محصول (/p/...) → افزودن به سبد (/cart)
    → صفحه سبد → تکمیل اطلاعات + آدرس
    → انتخاب درگاه پرداخت
    → انتقال به بانک → پرداخت موفق
    → بازگشت به سایت (/pay/result)
    → تأیید پرداخت (confirmPaymentOn):
        ✅ کسر از انبار (stock_movements)
        ✅ سند حسابداری خودکار (postSaleOn):
           - بدهکار: بانک (1200) / بستانکار: درآمد فروش (4100)
           - بدهکار: بهای تمام‌شده (4200) / بستانکار: موجودی کالا (1300)
           - بدهکار: بانک (1200) / بستانکار: ارزش افزوده پرداختی (3400)
        ✅ ثبت پرداخت (payments)
        ✅ تاریخچه وضعیت (order_status_history)
        ✅ رویداد (events: order.paid)
        ✅ ارسال خودکار به مؤدیان (اگر moadian_auto_send فعال باشد)
    → سفارش در وضعیت 'paid'
    → پنل مدیریت: بسته‌بندی → ارسال (+ کد رهگیری + برچسب PDF) → تحویل
```

**وضعیت: ✅ کامل — جریان بدون نقص بحرانی**

**جزئیات بیشتر:**
- ✅ قیمت‌گذاری: قیمت اصلی، قیمت ویژه، تخفیف، مالیات بر ارزش افزوده
- ✅ فیلترها: برند، نوع، رنگ، بازه قیمت، موجودی، سازگاری با گوشی
- ✅ مرتب‌سازی: جدیدترین، ارزان‌ترین، گران‌ترین، محبوب‌ترین
- ✅ مقایسه کالا (/compare)
- ✅ علاقه‌مندی‌ها (wishlist) ♡
- ✅ حساب کاربری: سفارشات، علاقه‌مندی‌ها، آدرس‌ها، پروفایل

---

## ۲. خریدار حضوری (صندوق POS)

```
مشتری → فروشگاه حضوری → فروشنده صندوق را باز می‌کند (openShift)
    → انتخاب کالا → ثبت فروش (sell)
    → انتخاب روش پرداخت:
        ✅ نقد (cash) → سند حسابداری: صندوق (1100)
        ✅ کارت‌خوان (card) → سند حسابداری: بانک (1200)
        ✅ کارت‌به‌کارت (card_to_card) → سند حسابداری: بانک (1200)
        ✅ پرداخت در محل (cod) → سند حسابداری: بانک (1200)
        ❌ چک — هنوز پیاده‌سازی نشده
    → ایجاد سفارش (channel: pos) + تأیید پرداخت فوری
    → سند حسابداری خودکار (postSaleOn)
    → فاکتور فروش PDF (GET /admin/exports/orders/:id/invoice.pdf)
    → بستن شیفت → مغایرت نقدی
```

**وضعیت: ⚠️ تقریباً کامل — نیاز به افزودن پرداخت با چک**

**شیفت POS:**
- ✅ افتتاحیه: موجودی اولیه نقد
- ✅ فروش: ثبت فروش + پرداخت فوری
- ✅ جابجایی نقد: برداشت، خرج، واریز
- ✅ اختتامیه: شمارش نقد + مغایرت (expected vs counted)
- ✅ خلاصه شیفت: فروش به تفکیک روش پرداخت

---

## ۳. همکار آنلاین (عمده‌فروشی اینترنتی)

```
همکار → ورود به حساب (is_partner: true)
    → مشاهده محصولات
    → قیمت همکار: ⚠️ تنظیمات partner_price_mode موجود ولی API قیمت متفاوت برنمی‌گردونه
    → سبد خرید → تکمیل سفارش
    → پرداخت: آنلاین یا چک
    → سقف چک (checkCeilingToman): ✅ در مدل مشتری موجود
    → اعتبار (creditRial): ✅ در مدل مشتری موجود
    → تکمیل سفارش مانند خریدار عادی
```

**وضعیت: ⚠️ نیاز به اتصال قیمت همکار به API**

---

## ۴. همکار حضوری (عمده‌فروشی فیزیکی)

```
همکار → فروشگاه → فروشنده صندوق را باز می‌کند
    → ثبت فروش + انتخاب مشتری (customerMobile)
    → پرداخت: نقد / کارت / کارت‌به‌کارت / چک
    → همان جریان POS
    → اتصال به حساب همکار: ⚠️ فروش POS مشتری را شناسایی نمی‌کند
```

**وضعیت: ⚠️ نیاز به اتصال POS به حساب مشتری/همکار**

---

## ۵. مدیر — تعریف محصول و دسته‌بندی

```
مدیر → پنل مدیریت → محصولات
    → ایجاد محصول: نام، slug، برند، توضیحات، تصاویر
    → تنوعات: رنگ، حافظه، قیمت، قیمت ویژه، بارکد
    → مشخصات فنی: کلید-مقدار (مثلاً صفحه‌نمایش: 6.5 اینچ)
    → سازگاری با گوشی‌ها (case_compatibility)
    → دسته‌بندی درختی: والد → فرزند → نوه
    → تصاویر + آپلود (upload-token)
    → مالیات: SSTID (شناسه یکتای مؤدیان)
    → حذف نرم (deleted_at)
```

**وضعیت: ✅ کامل**

---

## ۶. مدیر — تأمین و خرید (Procurement)

```
مدیر → درخواست خرید (Purchase Request)
    → انتخاب تأمین‌کننده
    → انتخاب کالاها + تعداد
    → رسید خرید (Purchase Receipt):
        → ورود به انبار + بهای میانگین موزون (WAC)
        → ثبت فاکتور خرید:
           → سند حسابداری خودکار:
              - بدهکار: موجودی کالا (1300) / بستانکار: حساب پرداختنی (2200)
              - بدهکار: ارزش افزوده خرید (3300) / بستانکار: حساب پرداختنی (2200)
    → شرایط تسویه:
        ✅ نقد (cash)
        ✅ اعتبار ۱۵ روزه (credit_15)
        ✅ اعتبار ۳۰ روزه (credit_30)
        ✅ چک (cheque)
    → فاکتور خرید PDF: ❌ هنوز پیاده‌سازی نشده (فقط در دیتابیس)
```

**وضعیت: ⚠️ تقریباً کامل — نیاز به PDF فاکتور خرید**

---

## ۷. مدیر — حسابداری

```
مدیر → دفتر کل
    → سند دستی: بدهکار / بستانکار + شرح + مرکز هزینه
    → برگشت سند
    → ترازنامه آزمایشی (Trial Balance)
    → صورت سود و زیان (P&L)
    → ترازنامه (Balance Sheet)
    → سند خودکار از فروش POS: ✅
    → سند خودکار از فروش آنلاین: ✅
    → سند خودکار از فاکتور خرید: ✅
    → فاکتور خرید: لیست + جزئیات + ردیف‌ها + بهای تمام‌شده
```

**وضعیت: ✅ کامل**

---

## ۸. مدیر — مالیات و مؤدیان

```
مدیر → تنظیمات مالیاتی
    → گواهی مالیاتی (Certificate): شماره اقتصادی، نوع شخص
    → صورتحساب مالیاتی: ثبت + ارسال به مؤدیان
    → ارسال خودکار پس از پرداخت (moadian_auto_send): ✅
    → شناسه یکتای مؤدیان (SSTID): ✅ در مدل محصول
    → ارسال دسته‌ای (batch send)
```

**وضعیت: ✅ کامل**

---

## ۹. مدیر — پیامک

```
مدیر → تنظیمات پیامک
    → ارسال‌کننده: کاوه‌نگار، ملی‌پیامک، فراز SMS
    → قالب‌های پیامک: ثبت‌نام، خوش‌آمدگویی، پرداخت، ارسال، تحویل
    → صف ارسال: ✅ (sms_queue)
    → نوتیفیکیشن سفارش: ✅ (POST /admin/email/notify-order)
    → اتصال خودکار به تغییر وضعیت: ⚠️ نیاز به اتصال
```

**وضعیت: ⚠️ نیاز به اتصال خودکار پیامک به وضعیت سفارش**

---

## ۱۰. مدیر — ارسال و مرجوعی

```
مدیر → سفارشات → بسته‌بندی (pack)
    → ارسال (ship): شرکت حمل + کد رهگیری + برچسب PDF/HTML
    → تحویل (deliver)

مرجوعی:
    مشتری → درخواست مرجوعی (return request)
    → تصمیم مدیر (approve/reject)
    → ارسال کالا (transit)
    → دریافت کالا (receive)
    → بازپرداخت (refund) + سند حسابداری
    → برگشت موجودی به انبار (restock)
```

**وضعیت: ✅ کامل**

---

## ۱۱. مدیر — گزارش‌ها

```
مدیر → گزارش‌ها
    → سفارش‌ها: XLSX + PDF
    → کالاها: XLSX
    → موجودی انبار: XLSX
    → مشتریان: XLSX
    → سود ناخالص: XLSX + PDF
    → داشبورد: خلاصه فروش، سفارشات اخیر، هشدارها
    → برون‌بری: JSON (orders, products, customers)
    → پیش‌بینی موجودی (forecast)
```

**وضعیت: ✅ کامل**

---

## ۱۲. مدیر — سایر عملیات

```
✅ کوپن‌ها و تخفیف‌ها: ایجاد، ویرایش، محدودیت زمانی/تعداد
✅ بنرها و اسلایدرها: تصویر، لینک، اولویت
✅ نظرات و امتیازها: تأیید/رد، پاسخ
✅ پرسش و پاسخ: پرسش مشتری → پاسخ مدیر
✅ جستجو: مترادف‌ها (synonyms)، خودکامل‌شونده (autocomplete)
✅ تنظیمات: فروشگاه، سفارشات، درگاه، مالیات، پیامک، پیشرفته
✅ نظارت: لاگ‌ها، health check
✅ احراز هویت: ورود، ثبت‌نام، نقش‌ها و دسترسی‌ها (RBAC)
✅ محدودیت نرخ (rate limiting): ورود، ثبت‌نام، پرداخت
✅ سقف OTP: ۵ تلاش + ۵ دقیقه قفل
```

---

## خلاصه نواقص شناسایی‌شده

| ردیف | نقص | اولویت | وضعیت |
|------|------|--------|-------|
| ۱ | پرداخت با چک در POS | 🔴 بحرانی | ✅ رفع شد — SellDto + UI + حساب تسویه (1400) |
| ۲ | قیمت همکار در فروشگاه | ⚠️ مهم | ✅ رفع شد — isPartner در search/list + SQL |
| ۳ | پیامک خودکار تغییر وضعیت | ⚠️ مهم | ✅ رفع شد — ۴ رویداد: پرداخت، ارسال، تحویل، لغو |
| ۴ | PDF فاکتور خرید | ⚠️ مهم | ✅ رفع شد — GET /accounting/purchases/:id/pdf |
| ۵ | اتصال POS به حساب مشتری | ⚠️ مهم | ✅ رفع شد — resolve customerId from mobile |
| ۶ | کارت‌خوان API | 🔵 مطلوب | 🔵 باقی — نیاز به اتصال سخت‌افزاری |

---

## ✅ ویژگی‌های موجود (تأیید شده)

| بخش | ویژگی‌ها |
|------|----------|
| **فروشگاه** | جستجو، فیلتر، مرتب‌سازی، مقایسه، علاقه‌مندی، سبد، پرداخت آنلاین |
| **حساب کاربری** | سفارشات، علاقه‌مندی‌ها، آدرس‌ها، پروفایل |
| **POS** | شیفت، فروش، جابجایی نقد، بستن شیفت، مغایرت |
| **سفارشات** | ایجاد، پرداخت، بسته‌بندی، ارسال، تحویل، مرجوعی کامل |
| **محصولات** | تعریف، تنوعات، مشخصات فنی، تصاویر، دسته‌بندی درختی |
| **تأمین** | تأمین‌کننده، درخواست خرید، رسید، فاکتور، شرایط تسویه |
| **حسابداری** | دفتر کل دوبل، ترازنامه آزمایشی، سود و زیان، سند خودکار |
| **مالیات** | گواهی، صورتحساب، ارسال مؤدیان، SSTID |
| **پیامک** | ۳ ارسال‌کننده، قالب، صف |
| **گزارش** | ۵ گزارش (XLSX/PDF)، داشبورد، پیش‌بینی |
| **امنیت** | RBAC، rate limiting، OTP، احراز هویت JWT |